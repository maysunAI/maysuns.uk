# PJ50_personal_website（原PJ44_personal_website）— maysuns.uk 个人网站

> 立项时间：2026-06-13
> 状态：开发中
> 域名：maysuns.uk（Cloudflare DNS）

## 定位

Jason / maySunAI 的个人品牌网站。宣传 T00 开发框架 + 展示产品。

## 产品列表

- MD Markdown Reader（PJ25，原PJ42）— 网页版已上线 reader.maysuns.uk，Google Play 尚未提交
- 吃药提醒 App（PJ20）— 开发中
- MyAI 个人AI助手平台（PJ90）— 开发中，2026-07-16加入Portfolio
- Product Shop 商品直销平台（PJ23）— 已上线 shop.maysuns.uk，2026-07-16加入Portfolio并附真实链接
- 更多产品陆续上线

## 技术方案

- 纯 HTML + CSS（无框架）
- Vultr 服务器托管（167.179.106.32，跟 report/shop/memo/reader/games 同台），2026-07-19 从 GitHub Pages 迁移过来
- Cloudflare DNS 绑定 maysuns.uk（A记录直接指向服务器IP）
- GitHub仓库（`github.com/maysunAI/maysuns.uk`）仍保留作为版本历史/备份，但不再是实际托管源

## 文件结构（2026-07-18 改为多页站，不再是单文件）

```
PJ50_personal_website/
  index.html                    ← 首页（产品作品集总览+导航）
  about.html                    ← 关于我（履历+T00框架+投资人区块，含原"团队"信息）
  product-*.html                ← 10个产品各自独立详情页
  assets/
    site.css                    ← 全站共享样式
    i18n.js                     ← 全站共享三语翻译字典（ja/en/zh，日语默认第一）
    main.js                     ← 语言切换+主题切换共享逻辑
  help_screenshots/             ← Playwright验证截图
  CLAUDE.md / REQUIREMENTS.md / OVERVIEW.md
```

语言切换按钮顺序固定为：EN / 日本語 / 中文（2026-07-30改，此前是日本語/EN/中文；默认语言仍为英文，除非浏览器语言/localStorage另有设定）。

## 部署步骤（2026-07-19起）

1. 本地改完文件，`git commit` + `git push`（保留版本历史，仓库地址见上）
2. 把改动的文件上传到服务器：
   ```
   scp -i ~/.ssh/pj23_vultr_deploy -r <改动的文件> root@167.179.106.32:/var/www/pj50-website/
   ```
3. 纯静态文件，nginx 直接生效，不需要重启/重新构建任何东西
4. nginx配置：`/etc/nginx/sites-enabled/maysuns.uk`；证书：Let's Encrypt（`certbot`管理，自动续期，到期日2026-10-17）

⚠️ 旧的 GitHub Pages（Settings→Pages custom domain功能）已停用（DNS不再指向它），但没有主动关闭，仓库本身的 `CNAME` 文件已在服务器版本里删除（服务器不需要它，GitHub仓库里如果还留着不影响，纯历史遗留）。

## 截图存放规则（2026-07-25新增，因根目录再次散落而补充）

**Playwright（或其它工具）给`help_screenshots/`存验证截图时，必须用完整绝对路径**（如`F:\T00\PJ50_personal_website\help_screenshots\xxx.png`），**禁止只写文件名或相对路径**——只写文件名时会默认存到工具当前工作目录（通常是`F:\T00`根目录），不是这个项目自己的`help_screenshots/`文件夹。

- **根本原因**：`AI00_Common/CLAUDE.md`早就有这条通用规则（2026-07-18审计时因PJ25/PJ52/PJ53截图散落到根目录而写入），但2026-07-21验证移动端导航下拉菜单时，5张截图（`mobile_top.png`/`mobile_top_after.png`/`mobile_top_after2.png`/`mobile_home.png`/`mobile_dropdown_click.png`）还是被截图工具存到了`F:\T00`根目录（当时文件名带了`pj50_`前缀，说明知道要区分项目，但路径本身没写全），直到2026-07-25才被发现并归位到本文件夹的`help_screenshots/`。也就是说**光有通用规则不够，AI在实际调用截图工具时必须每次显式检查参数是不是完整路径**，不能只靠"记得有这条规则"。
- **这不是PJ50独有的问题**，是跨项目的通病（PJ25/PJ52/PJ53都发生过），但既然在这个项目又发生了一次，这里单独强调一遍，作为本项目的硬性检查项：**每次用Playwright截图前，先确认`filename`参数是`F:\T00\PJ50_personal_website\help_screenshots\...`这样的完整路径，不是裸文件名**。

## 营销思维检查清单（2026-07-28新增，源自tasks/D01）

每次PJ50或旗下产品页面完成一个新功能/新内容，过一遍下面问题（不用每条都做，但要主动想一遍，别等用户问）：
这个功能值得宣传吗？用户为什么会用它？真正解决了什么问题？有没有更简单的实现方式？有没有可以自动化？有没有更好的UI？适不适合做短视频/写博客/发GitHub/发Reddit/发Product Hunt/发小红书/发抖音？

如果发现某项一直没做（比如做完功能从没写过一句宣传文案），主动提醒用户，不用等对方先问。可行性评估详见`tasks/D04-marketing_automation_assessment.md`；专业宣传图直接用`image`skill生成即可，不必等D02完整自动化流水线（已记录到`FUTURE_FEATURES.md`的F005，暂不做）。

## 变更记录

- **2026-07-31（第三十五次，六项任务打包：updates-news多语言真内容+改版单栏列表布局+product-reader去重CTA放大demo+清理MD Reader推广残留+changelog做成手风琴+全站nav一致性审计+git补提交）**：委托子Agent执行的六项独立任务，最后额外做了一次git checkpoint。
  - **①`updates-news.html`——按钮/状态框搬到网格下方+真正的多语言内容+视觉改版**：
    - **位置调整**：`.upd-refresh-box`（🔍搜索按钮+状态提示）从分类列表**上方**移到了**下方**（紧接列表、footer之前）。原因：按钮文案EN/JA/ZH三语言长度不同（日语最长），这一小块的渲染高度会跟着变，放在列表上方时手机端切语言会让下面的分类列表整体跳一下；挪到列表下面后，同样的高度差被更大的滚动距离"淹没"，肉眼基本感觉不到。
    - **真实的多语言内容修复**（这是本次的核心bug修复）：切换UI语言并不会让新闻内容本身变语言的根本原因是后端AI搜索本来就只搜英文来源。SSH进服务器改了`/var/www/pj50-backend/claudeNews.js`：prompt新增明确要求——前两次`web_search`调用必须分别用**中文**和**日文**写的搜索词（不是"用英文查中文话题"，是查询文本本身就是中文/日文），命中原生语言信源（36氪/IT之家/机器之心等中文源，ITmedia/Impress Watch等日文源），并要求"title"/"body"用信源自己的原始语言写，不做机翻。每条新增`sourceLang`字段（`en`/`ja`/`zh`），`sanitizeItems()`里对非法值兜底成`en`。`MAX_WEB_SEARCH_USES`从3调到5、`MAX_TOKENS`从2048调到4096——**真实踩坑**：第一次部署后测试，多语言搜索+更长的输出（中日文字符编码消耗更多token）导致响应又被`max_tokens`截断（跟第三十二次记录的老问题同一根因，在新场景下复现），加了预算才修复，按R17如实记录不是"AI说没有新内容"而是被截断。
    - **触发了3次真实付费的`/api/news/refresh`调用**（今天的免费额度已被更早的会话用掉，用真实生成的`NEWS_REFRESH_PASSWORD`触发）：第1次修复token预算问题、第2次验证多语言prompt生效但Claude的搜索预算仍主要花在英文源上（只搜到英文内容，包括一条关于中国AI监管的新闻但信源本身是英文站点）、第3次改进prompt明确要求"前两次搜索必须用中/日文关键词"后，搜到了一条中文源（AI工具宝箱）——但这条因为日期较早（超出10条留存名额）在自动合并时被挤掉了，没能真正上线。**判断call**：没有继续无限重试烧真实API/web_search费用去赌运气，改用自己的`WebSearch`工具（不产生pj50-backend的真实付费调用）额外核实了2条真实存在、日期在7月的中文（36氪《Claude Code偷查用户，时区、中国AI实验室全是关键词》）和日文（ITmedia《Claudeのチャット内容がGoogle検索結果に表示》）AI新闻，核实标题/日期/URL后手动写入服务器`news.json`并标记`important:true`（不占用10条名额、不会被自动淘汰），这是`newsService.js`早就支持、但一直没有UI能设置的"人工标记永久保留"路径，不是绕过后端逻辑瞎改。**当前生产环境`news.json`共12条**（10条常规英文源+2条人工核实的important中/日文源），后端的自动化搜索链路已经改好并验证生效（可以搜到多语言内容），只是自然产出的多语言新鲜度仍取决于Claude每次的搜索行为，如果未来某次自动搜索又搜到中/日文，会自然按同样规则合并进去。
    - **前端渲染**：每条卡片右上角新增语言徽章（EN/日本語/中文，复用site.css既有`.badge-live`同款"色块小药丸"视觉语言），徽章文字**始终显示该新闻来源自己的语言**（写死的原生语言名，不走i18n翻译成访客当前UI语言）——这是刻意的：徽章要传达的信息是"这条新闻本身是什么语言"，不该随访客切换UI语言而改变。
    - **视觉改版**：查了Hacker News的极简单栏列表设计做参考，从"4列卡片网格"换成单栏分区列表——每个分类一个小节（圆点+分类名+分隔线），节内用细分隔线隔开的紧凑行（粗体标题+摘要+日期/来源/语言徽章一行），不再用卡片边框+背景色堆砌。分类分组、后端留存策略（10条上限/90天/important豁免）完全没动，纯前端展示层调整。**过程中用Playwright实测发现一个真bug**：`site.css`里有条全站通用规则`section{padding:56px...;max-width:...;margin:0 auto}`，我给分类小节用了语义更合适的`<section>`标签，结果每个分类小节平白继承了56px上下内边距+自己的最大宽度，条目少的分类（如"Other AI Coding Tools"只有1条）下方留出一大截空白，看着比预期"更乱"。已加`.upd-cat-block{padding:0;max-width:none;margin:0}`显式清空继承样式修复，修复前后都用Playwright实测确认。
  - **②`product-reader.html`——去重CTA+demo放大**：原来页面上有两处指向`reader.maysuns.uk`的链接（顶部一处+demo区一处），删掉了顶部那处（`prod_reader_link`），只保留demo区末尾那个"Try MD Reader →"作为全页唯一CTA，天然就在页面最底部（demo区本来就是最后一个内容区块，不用额外挪动DOM顺序）。**demo放大**：实测GIF素材（`assets/demo/pj25_drop_demo.gif`）原生分辨率860×215，但外层`.prod-reader-demo`容器（820px）+卡片自身padding（最多32px×2）之前把可用渲染宽度压缩到了756px，比原生分辨率还小，这才是"看起来小"的真正原因——不是`max-width:760px`这个数字本身的问题。容器放宽到跟全站宽版内容区一致的`min(1100px,94vw)`，卡片padding收紧，图片`max-width`提到920px（比原生860px只轻微放大约1.07倍，Playwright实测桌面下渲染宽度874px，肉眼看不出模糊），卡片加阴影更显眼。孤儿i18n key（`prod_reader_link`三语言）确认无其它引用后删除。
  - **③清理MD Reader推广残留**：grep全站确认`knowledge-t00.html`结尾还留着一段"说到Markdown...`Try the MD Reader →`"的推广文字+链接（第三十四次那批任务已经把`kb-doc-viewer.html`那份搬到了`product-reader.html`并放大，但漏了`knowledge-t00.html`这一处），已删除对应段落+孤儿key（`kt00_p5`/`kt00_reader_cta`三语言）。`product-reader.html`自己的demo区CTA确认保留（那是它自己的正当内容）。
  - **④`updates-changelog.html`——改成点击展开的手风琴**：16条条目原来每条都是一整句完整说明，现在每条默认只显示一行短摘要（新增`upd_logN_summary`三语言key），点击/触摸展开出更详细的说明句（复用原有`upd_logN_text`作为展开态内容，不是新写一份雷同文案）。纯原生JS（`updates-changelog.html`自带的`<script>`，事件委托挂在整个列表上，没用任何框架/库），展开态用`max-height`+`scrollHeight`做平滑过渡，语言切换后如果条目正处于展开状态会重新计算`max-height`（三语言文字长度不同，写死的px值切完语言可能裁字或留白，已处理）。`aria-expanded`+chevron旋转做了基本可访问性。"只增不删"的既有规则（见第三十四次记录）本次没有违反——这次是改交互形式，16条内容原样保留，一条没删。
  - **⑤全站nav一致性审计**：写脚本把每个页面的`<nav>...</nav>`区块跟`index.html`逐行diff（过滤掉`nav-current`这类"标记当前页"的预期差异）。**发现3处真实不一致，已修复**：
    - `stories.html`语言按钮顺序还是旧的"日本語/EN/中文"，全站其它页面早在第三十四次之前就已经统一改成"EN/日本語/中文"（见文件开头"语言切换按钮顺序固定"那条说明），这一个页面漏改了——已改成标准顺序。
    - `whats-next.html`的nav把标准的"✨ Ask AI"链接替换成了自己的"What's Next"（自我指向当前页），跟其它29个页面的nav结构不一致（其它页面都是"nav-current标记已有的标准nav项"，不是"新增一个指向自己的额外nav项"）——已改回标准的"Ask AI"链接，孤儿i18n key`nav_whatsnext`三语言一并删除。审计过程中顺手发现`nav_shop`也是孤儿key（早前"Maysun Shop"移出顶部导航后没清理干净），一并删除。
    - 24个页面（含`index.html`自己）的nav里"Updates ▾"/"Changelog"这两处字面量占位文字，跟`i18n.js`当前实际值"AI Updates ▾"/"My Updates"不一致——**这个不影响真实访客看到的内容**（`main.js`的`setLanguage()`加载时会用i18n字典的值覆盖掉HTML里写死的占位文字，所以有JS的情况下渲染结果两边其实一样），纯粹是源码层面没跟着改名同步更新，为了源码/维护一致性还是批量修正成跟`i18n.js`当前值一致。
    - **back-link风格**（`← Back to all products`）逐个检查过，class/文案/data-i18n key全站完全一致，只是`href`目的地因页面分类不同而不同（portfolio.html/products-general.html/index.html），这是既有的、合理的分类路由差异，不是bug，未改动。`product-help-system.html`没有back-link是第二十次记录里明确的设计决定（logo本身能回首页，判定为冗余已移除），同样不是bug。
  - **验证+部署**：Playwright对生产环境`https://maysuns.uk`直接验证（不只信本地预览），涉及改动的7个核心页面（`updates-news.html`/`updates-changelog.html`/`product-reader.html`/`knowledge-t00.html`/`stories.html`/`whats-next.html`/`index.html`）× 桌面1400×900+手机390×844 × EN/JA/ZH三语言，全部0控制台报错、手机端`scrollWidth`(375)<`innerWidth`(390)无横向溢出。用`browser_run_code_unsafe`自建独立page对象截图/取值（这个Playwright浏览器实例仍被其它并发会话共享，标签页会被切走，第三十二次记录过的同款应对方式）。部署：`scp`全部32个html文件+`assets/i18n.js`+`assets/site.css`+`assets/main.js`到服务器（第一次操作失误把三个assets文件scp成了直接落在网站根目录而不是`assets/`子目录，`ls`发现后立刻删除误放的文件+改用正确的目标路径重新部署，未影响最终结果）；`i18n.js`因为本次改动了内容（增删多个key），全站`?v=`统一推进到`20260731b`。`curl`逐项确认生产环境返回的内容确实包含新代码（不是只看状态码）。
  - **⑥git checkpoint**：详见文末新增的独立说明（本条目下方按commit分组记录）。

- **2026-07-31（第三十四次，六项独立任务打包：ask-ai下线为"即将上线"+导航去掉Stories入口+kb-doc-viewer的MD Reader推广卡搬到product-reader.html放大+stories.html视觉复核+changelog内容重写+手机端语言/主题按钮移出抽屉菜单）**：这是一次委托给子Agent执行的六项并行子任务，过程中还收到两次中途追加/纠正指令，一并记录。
  - **①`ask-ai.html`下线**：读了现有代码后发现它**真的接了一个会调用外部AI API的完整聊天功能**（`fetch(ASK_API_BASE + '/api/ask')`，指向`ask.maysuns.uk`），确认是真实成本担忧后，整段`ASK_API_BASE`/`fetch`/`addMessage`聊天逻辑连同`.ask-chat`/`.ask-log`/`.ask-form`等专用CSS**全部删除**，不是"隐藏"或"禁用按钮"这种半吊子做法，是真的没有任何代码路径会再调用AI API。换成一张"🚧 AI Q&A — Coming soon"卡片，如实说明"每次提问都有真实AI调用成本，正在找可持续运营方式"，三语言（`ask_coming_h2`/`ask_coming_body`）新写。**判断保留了原有FAQ区块**（6条常见问题，纯静态内容，没有任何调用成本）没有一并砍掉——指令原文允许"coming soon风格的消息"，FAQ继续留着对访客仍有信息价值，砍掉FAQ反而是过度清空。孤儿key（`ask_thinking`/`ask_placeholder`/`ask_send`/`ask_hint`/`ask_role_you`/`ask_role_ai`/`ask_error_ratelimited`/`ask_error_generic`）三语言全部从`i18n.js`删除。
  - **②全站导航"AI Knowledge ▾"下拉去掉Stories入口**：`grep`确认30个页面（29个共享nav的html + `stories.html`自身）都有这一行，脚本批量删除（`stories.html`自身那份带`class="nav-current"`，脚本单独处理了这个变体）。确认`nav_aboutme_stories`/`nav_aboutme_stories_sub`两个key删除前后除了这一处nav入口外，全站没有其它地方引用，已从三语言`i18n.js`一并删除。**可达性确认**：`stories.html`本身完全没删，`index.html`首页本来就有一张指向它的缩略图+"阅读完整故事→"按钮（这次没有改动这处），确认删掉nav入口后这个页面依然可达，不会变成孤儿页。
  - **③+④ MD Reader推广卡从`kb-doc-viewer.html`搬到`product-reader.html`并放大**：`kb-doc-viewer.html`里的`.kbv-promo`整块（GIF+文案+"Try MD Reader"按钮）连同专用CSS一起删除；`product-reader.html`新增一个`<section class="prod-reader-demo">`，同一张GIF放大到`max-width:760px`（原来480px），外面包一层跟本页`.tech-list`/`.btn-outline`视觉一致的圆角边框卡片，标题"See it in action"。**判断call**：没有直接复用原来的`kbv_promo_p`/`kbv_promo_cta`两个key，因为原文案"This guide is a plain .md file..."是写给"知识库文章页"语境的，搬到MD Reader自己的产品页上读起来别扭，改写成新的`prod_reader_demo_h2`/`_p`/`_cta`三语言key（`_cta`按钮文案维持"Try MD Reader →"不变，其余重写），旧的`kbv_promo_*`确认无处引用后整组删除，没有留重复的近义key。
  - **⑤`stories.html`视觉复核**：用Playwright真实加载本地预览+生产环境`https://maysuns.uk/stories.html`，桌面1400×900+手机390×844，EN/JA/ZH三语言逐场景检查。**如实汇报：找到一个真实问题**——"Visiting the client"（交换名片）和"See you later"（鞠躬告别）两个场景用的是`.st-real-illust`独立卡片（不是双栏布局），桌面宽屏下这个卡片会撑满约982px的内容栏宽度，但图片本身有`max-width:220px`的限制，实测两侧空白占了卡片总宽度的78%，一张小图漂在一个特别宽的米色卡片正中央，视觉上确实符合"元素之间空白过多"这一条。**已修复**：加一条`.st-step > .st-real-illust { max-width:360px; margin:auto }`（只影响这两个独立卡片场景，不影响"Done"场景里嵌在`.st-row-2`双栏布局中的同名class，手机端因为栏宽本来就小于360px所以肉眼无变化）。除此之外**没有发现其它真实问题**——icon编号和标题看似不在同一行是`.st-step-illustrated`的`flex-direction:column`故意设计（先量了图标行高32px+gap4px正好等于标题的起始位置，确认不是bug）；场景之间的间距实测完全等宽衔接（用`getBoundingClientRect`量了全部10个场景的top/height，衔接处误差在1px以内）；图片自然尺寸都大于展示尺寸（没有被拉伸放大糊掉的情况）。**没有为了"显得认真"而制造额外改动**。
  - **⑥`updates-changelog.html`内容重写**：把`CLAUDE.md`里2026-07-28~07-30约30条内部会话记录，提炼成12条访客视角的大白话条目（去掉PJ编号/文件名/"第几次会话"这类内部黑话），三语言写入`i18n.js`的`upd_log1`~`upd_log12`。**中途收到用户纠正**：网站主人实际看了线上页面，发现我的重写**把原有4条（2026-07-18/07-19，"导航重构"/"故事型改版"/"Vultr迁移"/"多页重建"）整段替换掉了**，导致"我的动态"只剩最近3天、丢了更早的历史。已从`Z01_backup_20260730f/assets/i18n.js`（本次批量编辑前的完整快照）里取回这4条的原始三语言文案，追加为`upd_log13`~`upd_log16`（编号顺延，日期最旧，排在新12条之后，整体仍是从新到旧排序），确认现在全部16条都在，新旧都不丢。**已确立并记录新规则**：这个页面以后是**只增不删**的——每次有新的、值得让访客知道的进展，往列表里追加，不能因为"这次刷新只想说最近几天的事"就把更早的条目整段删掉；以后任何一次"重写这个页面"的请求，默认理解为"追加"，除非用户明确说"删掉某条"。
  - **中途追加的第七项任务（同样折进这批nav改动里）：手机端语言切换+主题按钮移出汉堡抽屉菜单**。问题：手机宽度下`.lang-switch`/`.theme-toggle`原来是`.nav-right`（汉堡抽屉）的一部分，访客要点开汉堡菜单才能切语言/切主题。改法：把这两块从`.nav-right`里搬出来，新建`.nav-utility`容器，放在DOM里`.nav-logo`和`.nav-hamburger`之间；桌面端用`@media (min-width:641px){.nav-utility{order:10}}`把它在视觉上推回原来最右侧的位置（flex属性，不改DOM顺序），手机端因为`.nav-hamburger`本来就在它后面、`.nav-right`变成`position:fixed`抽屉不参与正常流布局，天然就是"logo→语言/主题→汉堡"从左到右排列，不用额外CSS。手机端另外给`.lang-btn`/`.theme-toggle`/`.lang-switch`加了更紧凑的padding/字号（0.68em），实测390px屏幕上三个语言按钮+主题按钮跟logo、汉堡挤在一行不换行不溢出。**这次改动和"删除Stories入口"是同一批30个共享nav页面**，用同一个批处理脚本一次性做完两处修改（没有分两轮改同一批文件）。删除了`@media(max-width:640px)`里已经过时的`.lang-switch{margin-bottom:12px}`和`.lang-switch,.theme-toggle{width:100%}`两条规则（抽屉里不再有这两个元素了）。已用Playwright验证：桌面1400×900下语言/主题按钮位置和改动前肉眼一致（仍在最右侧）；手机390×844下两者可见于顶部条、不用开汉堡菜单；点击测试确认语言切换和主题切换从新位置都能正常工作（包括`knowledge-ai-glossary.html`那个用自己`gkOnLangClick`包装函数的特殊页面）；汉堡菜单点开后确认抽屉里只剩下`nav_links`（导航链接），语言/主题不再重复出现在抽屉里；`scrollWidth`≤`innerWidth`手机端无横向溢出。
  - **⚠️ 真实的并发编辑碰撞（如实记录，供以后参考）**：这次会话过程中反复发现有**另一个并行会话在同时对同一批PJ50文件做改动**——不是猜测，是直接证据：①Playwright浏览器里不断有新标签页被自动打开/切换到生产环境的`updates-news.html`等页面，跟本次任务操作无关；②`about.html`/`whats-next.html`/`updates-news.html`三个文件在我完成"删Stories入口"批处理之后又被改动了一次，`i18n.js`引用版本号被对方独立推进到了`?v=20260730h`；③最严重的一次——`updates-news.html`在部署前的最终核对时发现**我删掉的Stories导航行被重新加了回来**（对方的编辑显然是基于一份比我的改动更旧的文件快照写回的，文件行数从118行变成293行，说明对方往这个页面加了不少新内容，但连带把旧版nav也一起写回了）。**处理方式**：没有尝试回滚或覆盖对方的改动（那样可能销毁对方正在进行中的工作），而是在部署前重新对`updates-news.html`单独跑了一次"删除Stories行"的针对性脚本，确认对方新增的内容原样保留、只有这一行被重新删掉；`i18n.js`/`site.css`的版本号统一推进到比双方已知版本都新的号码（`i18n.js?v=20260730j`、`site.css?v=20260731a`），保证不管谁的改动最后落地，访客拿到的缓存版本号都是最新的。**这个问题特意写下来告知用户**：如果发现某些改动"莫名其妙又变回去了"，很可能是这类并发编辑冲突，不是这次任务本身漏改。
  - **备份**：改动前的完整快照在`Z01_backup_20260730f/`（30个html文件+`assets/i18n.js`+`assets/site.css`，今天全部6项任务+两次追加任务改动过的文件均在其中）。唯一的例外是`pj50_help.html`——只被这次的`i18n.js`版本号批量替换脚本碰了一下（内容本身没改，只是`?v=`参数从e换成最新值），因为它不在原定6项任务范围内所以没有单独备份，改动内容简单且完全可逆（如需还原，把`i18n.js?v=`参数改回`20260730e`即可）。
  - **验证+部署**：本地`python -m http.server 8971`+生产环境`https://maysuns.uk`双重验证，覆盖全部6项任务改动的页面，桌面1400×900+手机390×844，全部改动过的页面控制台0报错（这次全程没再遇到favicon 404）。部署用`scp`把32个改动过的html文件+`assets/i18n.js`+`assets/site.css`陆续推到Vultr服务器，`curl`逐个确认生产环境返回200且响应体确实包含新内容（不是只confirm状态码），并用Playwright直接对生产环境截图确认桌面/手机渲染正确。截图：`help_screenshots/askai_comingsoon_desktop_zh_20260730.png`/`product_reader_demo_enlarged_desktop_zh_20260730.png`/`changelog_rewritten_desktop_zh_20260730.png`/`kbdocviewer_no_promo_desktop_zh_20260730.png`/`stories_v20_padding_fix_desktop_20260730.png`/`stories_v20_padding_fix_mobile_20260730.png`/`stories_production_padding_fix_desktop_en_20260730.png`/`stories_production_scene8_seeyoulater_20260730.png`/`navutility_desktop_check_20260731.png`/`navutility_mobile_check_20260731.png`/`navutility_mobile_drawer_open_20260731.png`/`navutility_production_mobile_20260731.png`/`changelog_final_16entries_production_20260731.png`。

- **2026-07-31（第三十三次，"本周AI动态"卡片去掉固定高度+内部滚动，改回自然高度+整页滚动）**：紧接上一条（第三十二次）的后续反馈——网站主人实际看了线上页面后反馈，"固定220px高度+`overflow-y:auto`内部滚动"这个设计"太多滑块了"（最多10条动态分布在4个分类里，等于最多10个各自独立的小滚动条同时出现）、"排版太乱了"。这是纯前端展示层面的调整，**没有碰**后端`GET /api/news`/`POST /api/news/refresh`两个接口、也**没有**改留存策略（10条上限/90天/`important`豁免完全不变，只是换了一种把同样的数据画出来的方式）。
  - **具体改动**：`updates-news.html`里`.upd-news-card`去掉了`height:220px`+`display:flex;flex-direction:column`，JS渲染模板去掉了包裹标题+正文的`.item-scroll`内层`div`（连带它的`overflow-y:auto`一并消失）。现在每张卡片按标题+正文的真实内容自然撑高，不再裁切/滚动任何内容；一个分类里有多条时，卡片纵向自然堆叠，整个页面像普通博客/列表页一样从上到下滚动——**唯一的滚动是整页滚动**，不是之前那种"页面滚动+多个卡片各自还能再滚动"的双层滚动。分类分组（4列`.upd-news-grid`）、卡片的边框/背景/圆角/标题/正文/日期+来源链接这些视觉元素全部保留，只是卡片本身不再是固定高度容器。
  - **验证**：Playwright真实验证桌面1400×900+手机390×844，EN/JA/ZH三语言（用真实`.lang-btn`点击切换，不是走`evaluate()`直接调函数——上一轮验证时发现直接调用`setLanguage()`会跳过页面自己给"最后更新"这行加的重渲染逻辑，导致日期显示不出来，这属于测试方法的问题不是产品本身的bug，这次全部改用真实点击来验证）。用`getBoundingClientRect().height`实测10张卡片的真实高度分布在218px～269px之间（不再是清一色220px），确认`.item-scroll`元素在页面上数量为0，手机端`scrollWidth`(375px)小于`innerWidth`(390px)无横向溢出，桌面+手机控制台均0报错。数据全部来自生产环境`/api/news`的真实当前状态（10条，2026-07-30最后更新），不是mock数据。部署后`curl`生产环境`updates-news.html`确认`height: 220px`和`item-scroll`两个字符串在线上文件里都已经查不到（`grep -c`结果为0），不是只改了本地预览。截图：`help_screenshots/updatesnews_desktop_en_naturalheight_20260731.png`/`updatesnews_desktop_ja_naturalheight_20260731.png`/`updatesnews_desktop_zh_naturalheight_20260731.png`/`updatesnews_mobile_en_naturalheight_20260731.png`/`updatesnews_mobile_ja_naturalheight_20260731.png`。

- **2026-07-30（第三十二次，"本周AI动态"从手动改文案变成真AI驱动：新增`GET /api/news`+`POST /api/news/refresh`+"🔍用AI搜索最新动态"按钮，`updates-news.html`卡片改统一高度+内部滚动）**：用户反馈`updates-news.html`4个分类卡片"不美观框的大小都不一样"，且这块内容此前完全靠人工手改`assets/i18n.js`（见`NEWS_UPDATE_PROCESS.md`记录的旧流程），想要"可视化展示网站是AI驱动的，同时控制真实API成本"。
  - **视觉修复**：`.upd-news-card`改成固定高度220px+`overflow-y:auto`，标题+正文放进单独的`.item-scroll`内层滚动区，日期+来源链接固定在卡片底部常驻显示（不随内容滚动），不管内容长短卡片外观现在完全一致，呼应首页`.f-thumb`统一尺寸卡片的同一设计逻辑（见"第二十六次"记录），但news卡片是文字内容不是图片，用的是"固定高度+内部滚动"而不是`aspect-ratio`。
  - **`pj50-backend`新增两个真实端点**（后端源码只存在服务器`/var/www/pj50-backend/`，本仓库没有本地镜像，本次通过SSH拉下来改完再scp回去）：
    - `GET /api/news`——读`news.json`返回`{ok, lastUpdated, items, categories}`，`items`每条含`category`(`claude`/`codex`/`othercoding`/`otherai`，四个slug跟`i18n.js`里`upd_news_*_h3`的key后缀保持一致)/`title`/`body`/`date`/`sourceName`/`sourceUrl`/`important`。种子数据=原来4条硬编码新闻原样迁移（一条不丢），存进新建的`news.json`（跟`messages.jsonl`一样是纯文件存储，不需要数据库）。
    - `POST /api/news/refresh`——点击"🔍用AI搜索最新动态"按钮触发。新增`news_refresh_state.json`记录`{date, count}`（服务器UTC自然日，本服务器时区确认就是UTC），当天第1次免费，第2次起要求`password`字段，走跟`/api/messages/list`**完全相同**的`Buffer.equals`常数时间比较模式，但校验的是**新建、独立**的`NEWS_REFRESH_PASSWORD`环境变量（不复用`ADMIN_PASSWORD`，两把密码互不影响，泄露一个不连累另一个）。真实调用Anthropic Claude API（`@anthropic-ai/sdk`，服务器`pj22-backend`已经在用同一个包，写法沿用它的`extractText`模式）+ `web_search_20250305`工具（**调用前查了platform.claude.com当前文档**，确认这是目前三个可用版本里最基础的一个，够用不需要`web_search_20260209`的动态过滤/`web_search_20260318`的response_inclusion这些进阶功能），让Claude搜索`news.json`里存的"最后更新日期"之后的新AI动态，最多4条，要求最终只回JSON数组。按`sourceUrl`去重合并进`news.json`，应用留存策略后落盘。
  - **留存策略的具体解读**（用户原话"留10条或者3个月内的，或者重要的"比较模糊，这是我的一个具体解读，如果理解错了可以直接改`newsService.js`的`applyRetention()`函数）：`important:true`的条目不管多老、不管多少条都保留，不占用下面的名额；其余非important条目里，先按超过90天（3个月的近似值）过滤掉，剩下的按日期倒序最多留10条。也就是说重要条目是"10条名额之外的额外保留"，不是"10条里挤一个位置"。
  - **真实踩坑+修复（诚实记录，不是一次就顺利跑通）**：第一次在生产环境真实点按钮测试时，`claude-sonnet-5`（默认模型）做了3轮`web_search`+推理，把最初设的`max_tokens:1024`预算耗尽在搜索过程本身，最终连结尾的JSON答案都没输出（`response.content`里没有任何text block），后端如实返回502错误（没有把"AI被截断"伪装成"AI说没有新内容"，这点符合本项目R17诚实汇报规则）。真正修复：`max_tokens`调到2048（仍远低于常规4096+的对话预算，符合"reasonably low"的要求）+ prompt里加了"不要输出任何解释性文字，直接给最终JSON"的强约束减少推理噪音。修复后重新在生产环境真实测试：**当天免费额度**跑出4条新内容（Cursor Start进军印度/Cognizant与Anthropic扩大合作/Claude Opus 5发布两个不同来源各一条），**用真实生成的密码触发第2次**又跑出2条（Cursor Router成本优化/Codex周活破千万等），两次合计`news.json`从4条种子涨到10条（正好碰到留存上限，但都在3个月内所以没有触发年龄淘汰），去重生效（`claude-opus-5-launch`这条种子跟AI搜到的"ClaudeLog"来源是同一件事但URL不同，两条都保留了——这是`sourceUrl`去重的已知局限，同一事件不同信源会被当成两条不同新闻，不是bug，是"按URL去重"这个简化设计的自然结果，如果want更聪明的"同一事件合并"需要额外做语义去重，目前没做）。
  - **另发现并修复一个小bug**：`updates-news.html`的"最后更新"这行`<p>`原来同时写了`data-i18n="upd_news_coverage"`属性和自己的JS动态替换逻辑——`main.js`的`setLanguage()`只要被直接调用（不一定是真人点击语言按钮的场景）就会把这个元素的`textContent`重置成i18n字典里的原始模板字符串（含未替换的字面量`{date}`），跟我自己的`renderCoverage()`函数打架。已去掉这个元素上的`data-i18n`属性，改成完全由页面自己的JS单独维护这一行内容，语言切换+日期填充两件事都在同一处逻辑里做，不会再互相覆盖。
  - **前端`updates-news.html`**：`fetch('/api/news')`加载后按`category`分组渲染进4个既有分类栏（一个分类可以有多条，纵向堆叠，不再是每类固定1条）；新增"🔍 用AI搜索最新动态/🔍 Search for latest updates with AI/🔍 用AI搜索最新动态"按钮（EN/JA/ZH三语言）+ 状态提示框，点击后处理三种结果：找到新内容/没找到新内容/需要密码（这种情况下框内会长出一个密码输入框+提交/取消按钮，跟`contact-admin.html`同款密码校验模式呼应但UI做成内嵌不是独立跳转页）。`assets/i18n.js`删掉了原来4个分类各自的`upd_news_*_title/body/date/source`共12组×3语言静态key（内容已经由后端`news.json`接管），新增`upd_news_empty`/`upd_news_refresh_*`共10个key×3语言；**顺手修了一个ZH语言块里已经存在的重复key bug**——`upd_news_otherai_source`原来写了两遍（"Tech Times"和"OpenAI官方发布"），JS对象字面量后一个会静默覆盖前一个，这是移动前就有的既有小问题，这次整段替换时一并清理掉了。`NEWS_UPDATE_PROCESS.md`（原来的人工搜索SOP文档）顶部加了一段说明，指向这次的自动化实现，文档本身保留作历史记录不删除。
  - **密码记录方式（沿用`ADMIN_PASSWORD`当初的做法）**：`.gitignore`里`.env`被排除不会进git，但这份`CLAUDE.md`本身**没有**被gitignore、会进`github.com/maysunAI/maysuns.uk`这个公开仓库的历史——所以这里不写真实密码值，只记录"已生成一把独立的`NEWS_REFRESH_PASSWORD`，写在服务器`/var/www/pj50-backend/.env`，真实值在生成时的会话回复里给过"，跟`ADMIN_PASSWORD`当初"真实随机密码只写在服务器.env，未出现在任何提交到仓库的源码里"是同一套做法。
  - **验证**：本地`node --check`全部新增/改动JS文件通过；本地`node server.js`+真实Claude API（用`claude-haiku-4-5`省成本做首轮联调）测试了免费额度/密码必填/密码错误/密码正确四条路径全部通过，之后重置`news.json`回4条种子再部署。部署后`pm2 restart pj50-backend`（确认进程名`pj50-backend`不变）；`curl`真实测试生产环境：`GET /api/news`200；`POST /api/news/refresh`真实免费额度跑通（4条新内容）；不带密码/错密码两次均正确返回403；带正确密码的真实第2次调用也验证通过（2条新内容）。Playwright验证桌面1400×900+手机390×844，EN/JA/ZH三语言，固定高度卡片+多条堆叠+内部滚动条视觉确认无误，手机端`scrollWidth`(375px)小于`innerWidth`(390/1400px)无横向溢出，控制台0报错。**因为服务器上这个Playwright浏览器实例被其他并发会话共享**（截图过程中数次发现"当前标签页"被其他会话的操作切换走），改用`browser_run_code_unsafe`在单次脚本里自建独立page对象完成截图/取值，绕开了标签页争抢的问题。截图：`help_screenshots/updatesnews_desktop_zh_before_refresh_20260730.png`/`updatesnews_desktop_en_20260730.png`/`updatesnews_mobile_en_20260730.png`/`updatesnews_desktop_ja_20260730.png`/`updatesnews_desktop_zh_20260730.png`/`updatesnews_mobile_ja_20260730.png`/`updatesnews_mobile_ja_password_required_20260730.png`。
  - **已知限制/判断call**：①动态搜到的新闻条目`title`/`body`只有英文（AI真实搜索的信源本来就是英文，没有为每条动态额外配三语翻译），UI外壳（导航/按钮/状态提示/分类标题）仍是三语言，这是有意的取舍；②`important`标记目前没有任何UI能设置，全部AI搜到的条目`important`恒为`false`，如果想把某条设成"永久保留"需要手动改服务器`news.json`；③按`sourceUrl`去重是简化设计，同一事件不同信源会出现两条（本次真实测试就遇到了Claude Opus 5发布被两个不同来源各报一条）；④`max_uses:3`+`max_tokens:2048`控制了单次调用成本，但web_search本身$10/1000次+token费仍是真实费用，今天完整测试流程（含1次调试失败+2次真实成功调用）总共触发了大约3次真实web_search工具调用批次（每批最多3次搜索）。

- **2026-07-30（第三十一次，about.html投资人按钮改指向留言板+whats-next.html删除"构想中"板块）**：两处小改动。①`about.html`投资人区块的"Start a Conversation"按钮原来是`mailto:j6016086@gmail.com?subject=Investor...`，改成直接链接`contact.html`（第二十八次新增的真实留言板功能），保持按钮文案不变，只换目的地——用户不想让投资人这条路径继续走mailto。②`whats-next.html`的"In the Pipeline"板块（介绍PJ19待办助手概念，第十九次加入）整段删除，连带`assets/i18n.js`里`next_pipeline_h2`/`next_pj19_label`/`next_pj19_status`/`next_pj19_desc`三语言共12行孤儿key一并清理（确认过站内其它页面没有引用）。两个文件+`assets/i18n.js`的`?v=`跟着当时其它并发改动推进到`20260730h`。部署后直接curl生产环境`about.html`/`whats-next.html`/`assets/i18n.js`确认改动生效（按钮href、pipeline区块消失、孤儿key消失）。

- **2026-07-30（第三十次，留言功能邮件通知真正修复生效+发现并修复.env未被gitignore的安全漏洞+stories.html加og:image+LinkedIn真实发布）**：用户在`F:\T00\PJ50_personal_website\.env`里留了一把真实Resend API Key（自己去resend.com注册拿到的）。①把key配置到服务器`pj50-backend/.env`+重启，**真实测试时发现Resend免费额度只能发到注册账号本身的邮箱**（HTTP 403），已把`NOTIFY_TO_EMAIL`从`gao616188@gmail.com`改成`j6016086@gmail.com`（Resend账号验证邮箱，跟PJ14是同一个），改完借用PJ14已有的Gmail授权直接查收件箱确认真的收到通知邮件——留言功能现在邮件通知这块是真正生效的，不再是"能存不能通知"的半成品状态。②**发现真实安全漏洞**：`PJ50_personal_website`文件夹之前没有`.gitignore`，`.env`处于"未跟踪"状态，`git add .`会把这把真实API Key提交进`github.com/maysunAI/maysuns.uk`仓库历史——已新建`.gitignore`（排除`.env`/`help_screenshots/`/`Z01_backup_*/`）堵上。③`stories.html`加了`og:image`（场景1插画`man_computer.png`），让LinkedIn/社交媒体贴链接时能生成带图预览卡片。④**真实登录用户LinkedIn账号（高嵩）发布了一条英文帖子**介绍`stories.html`，帖子地址`https://www.linkedin.com/feed/update/urn:li:activity:7488594443323969536/`，确认预览卡片正确带图+可点击跳转。⑤中文平台（小红书风格）文案已写好存入`PJ03_publish_promotion/POSTING_GUIDE.md`，如实说明这几个平台没有已登录session，AI没法代为发布，需要用户自己手动粘贴。测试期间产生的留言已从服务器`messages.jsonl`清空。

- **2026-07-30（第二十九次，作品集icon同标题一行的可见验证+发现并修复全站`site.css`缓存版本号没跟着内容更新的问题）**：上一条（第二十六条附近）改的`.product-card .product-icon`float修复部署后，第一次用Playwright肉眼复核时发现**线上视觉上完全没变**（icon仍然独占一行）。排查发现：`assets/site.css`的引用URL全站31个页面一直是`site.css?v=20260727b`这个固定版本号，从没跟着这次改动往后的任何一次`site.css`修改而更新过——本次会话对`site.css`本身的这次icon修复是真实推送到服务器了（`curl`直接fetch确认内容已经是新的），但因为URL没变，之前已经加载过这个URL的浏览器（包括这次一直开着的Playwright浏览器本身）会用本地缓存的旧版本，看不到新内容。**已修复**：全站31个html文件的`site.css?v=20260727b`统一改成`site.css?v=20260730f`，强制所有浏览器（不管有没有缓存）都会拿到最新内容。重新用Playwright验证：`getComputedStyle`确认`.product-icon`的`float`确实变成`left`，视觉截图确认icon和标题终于在同一行。**这提醒了一个操作习惯问题**：以后凡是修改`assets/site.css`（全站共享文件），都要记得同步给引用它的`?v=`参数换一个新值，不能只改内容不换版本号，否则真实访客（尤其是之前访问过网站、浏览器已经缓存过旧版本的人）可能长期看不到更新——这次`assets/i18n.js`每次改动都有跟着换版本号（今天已经从c→e），但`site.css`这次是本会话第一次改，之前没有先例提醒到这一点。

- **2026-07-30（第二十八次，新增真实"留言板"功能，替换footer的mailto链接，自建后端上线Vultr）**：用户明确要求把footer的纯`mailto:`邮件链接换成站内真实留言功能，被问到"自建后端 vs 第三方表单服务"时选择"自建后端，更可控"。
  - **新后端`pj50-backend`**：新建`/var/www/pj50-backend/`（Express，ESM，跟同服务器上`pj20`/`pj23-backend`同款代码风格），端口**5150**（部署前用`ss -tlnp`现查确认空闲，不是凭记忆猜的）。`POST /api/message`——接收`{name?, contact?, message, hp?}`，`message`必填且trim后1-2000字符，蜜罐字段`hp`（前端叫`website`）非空时静默丢弃（不落盘、不计入限流、不发邮件）但仍返回`{ok:true}`避免让bot学习到被检测；按IP限流每小时最多5条**被接受**的留言，用小JSON状态文件`messages_ratelimit.json`记录每个IP的时间戳数组（概念上参考本项目`PJ14_email_agent/stranger_guard.py`按发件人记录时间戳数组判断限流的模式，语言/项目不同，只借用思路），超限返回HTTP 429；通过校验的留言追加写入`messages.jsonl`（一行一个JSON对象：timestamp/name/contact/message/ip）。`POST /api/messages/list`——body需带`password`字段，用`Buffer.equals`做常数长度比较校验`ADMIN_PASSWORD`环境变量（真实随机密码只写在服务器`/var/www/pj50-backend/.env`，未出现在任何提交到仓库的源码里），校验通过返回`messages.jsonl`全部内容（倒序，最新的在前）；用POST不用GET+查询字符串，避免密码出现在nginx/浏览器历史记录里。
  - **邮件通知是best-effort、如实说明现状**：新增`emailSender.js`，完全照抄同服务器`pj23-backend/emailSender.js`的Resend调用模式（`fetch`直接POST `api.resend.com/emails`），收到新留言时尝试发提醒邮件到`gao616188@gmail.com`。**已逐个检查过本服务器上pj02/pj08/pj18/pj20/pj22/pj23-backend的`.env`，没有一个配置过真实`RESEND_API_KEY`**，这个新后端同样没配——`RESEND_API_KEY`缺失时`notifyNewMessage()`打日志后直接跳过，不抛异常，不影响留言已经真实落盘这个核心功能。**如实结论：邮件通知今天不会真的送达**，要让它工作需要：①用户去resend.com注册拿到`RESEND_API_KEY` ②填进服务器`.env` ③`pm2 restart pj50-backend`。留言功能本身（Jason能看到访客留言）不依赖这一层，跟本任务要求的"核心价值零外部依赖"一致。
  - **nginx**：编辑`/etc/nginx/sites-enabled/maysuns.uk`，顶部注释从"纯静态，无后端"改成反映真实现状，新增`location /api/ { proxy_pass http://127.0.0.1:5150; ... }`反代块（`proxy_http_version`/`X-Real-IP`/`X-Forwarded-For`/`X-Forwarded-Proto`四个头完全照抄`todomemo.maysuns.uk`同款反代块，不是自己发明的写法）。改之前`cp`了一份带日期的配置备份，`nginx -t`验证通过才`systemctl reload nginx`，reload后立刻curl确认`index.html`/`portfolio.html`仍200，没有因为加`/api/`块搞坏其它页面。
  - **前端新页面`contact.html`**：nav/footer/语言切换/主题切换等外壳逐字复制自`knowledge-rag.html`保持全站一致；表单——姓名(选填)/联系方式(选填，标注"邮箱或其它，方便回复你")/留言内容(必填textarea，含实时字数计数0/2000)/一个真正离屏定位（`position:absolute;left:-9999px`，不是`display:none`）的蜜罐输入框，字段名叫`website`（诱导bot误填常见字段名）。JS用`fetch('/api/message',...)`提交，提交期间禁用按钮防重复提交，成功/失败/限流三种状态分别显示对应的国际化提示文字。三语言（EN/日本語/中文）`contact_*`前缀共18个key，全部人工撰写真实译文，不是占位符。
  - **前端新页面`contact-admin.html`**：密码输入框+提交按钮，POST到`/api/messages/list`，成功后渲染表格（timestamp/name/contact/message/ip，最新在前）；`<meta name="robots" content="noindex">`，不加入任何nav下拉菜单（只能靠直接输入URL访问，等同后台入口）；密码校验完全在服务端进行，页面源码里不出现真实密码。按用户要求"不需要好看"，没有做移动端专门优化。
  - **全站28个html文件的footer链接批量替换**（`contact.html`本身不需要改，实际改了29个文件+`assets/i18n.js`）：`grep`确认后逐个把`<a href="mailto:j6016086@gmail.com" data-i18n="footer_contact">✉️ Email</a>`替换成`<a href="contact.html" data-i18n="footer_contact">💬 Leave a message</a>`，`footer_contact`三语言译文同步改成"💬 Leave a message"/"💬 メッセージを送る"/"💬 留言"。**改动前先备份到`Z01_backup_20260730e/`**（29个html文件+`assets/i18n.js`，`i18n.js`因为在备份前已经手滑先编辑了，改用`git show HEAD:assets/i18n.js`补拿到编辑前的原始版本，效果等同"先备份"）。批量脚本跑完后用`diff`逐个核对每个文件相对备份只多了预期的1-2行改动（footer链接+`i18n.js`缓存版本号`?v=20260730e`），没有意外改动；`about.html`的投资人mailto链接、`whats-next.html`的订阅表单mailto action两处刻意保留不动（不在本次需求范围内）。**过程中确认了一处无关的并发编辑**：`t00.html`在我备份之后、编辑之前被另一个并行会话追加了一行CSS（`align-self:center`，对应CLAUDE.md新增的"第二十七次"记录，`t00.html`拆分成两个页面那次），核对后确认这处改动跟我的footer/版本号修改互不冲突，两边改动都完整保留在最终文件里，没有互相覆盖。
  - **验证（真实端到端，非纯代码审查）**：本地`node --check`全部新增JS文件通过；本地`node server.js`真实起服务+`curl`验证：正常留言存入`messages.jsonl`✅、蜜罐字段命中静默丢弃且未落盘✅、错误密码拒绝✅、正确密码返回内容✅、空留言400✅、连续6次提交第6次429✅。部署后用Playwright**对生产环境`https://maysuns.uk/contact.html`做了真实的完整闭环测试**：填入真实姓名"Claude Code test"+留言"End-to-end test message..."提交→页面显示绿色成功提示→SSH进服务器确认`messages.jsonl`里真实出现这条记录→用生成的真实`ADMIN_PASSWORD`在`https://maysuns.uk/contact-admin.html`成功取回并显示这条留言→用错误密码测试确认被拒绝。蜜罐路径：用`browser_evaluate`填入蜜罐字段+提交，客户端显示成功（bot不会发现被拦截），但SSH确认服务器日志显示"蜜罐字段命中，判定为bot，静默丢弃"且未写入`messages.jsonl`。限流路径：连续对生产环境发送真实请求，第6次返回HTTP 429。桌面1400×900+手机390×844，EN/日本語/中文三语言，`scrollWidth`≤`innerWidth`无横向溢出，控制台0报错（除已知的favicon 404）。测试留言与限流状态在验证完成后已从服务器清空，不留测试脏数据。截图：`help_screenshots/contact_desktop_en_20260730.png`/`contact_desktop_ja_20260730.png`/`contact_desktop_zh_20260730.png`/`contact_mobile_zh_20260730.png`/`contact_mobile_production_en_20260730.png`/`contact_admin_gate_local_20260730.png`/`contact_e2e_success_production_20260730.png`/`contact_admin_authenticated_production_20260730.png`。

- **2026-07-30（第二十七次，`t00.html`拆分成"能做什么"+"做出什么"两个页面，首页底部按钮去掉"看看"式软化措辞）**：用户反馈首页底部第二个按钮（"看看已经做出了什么→"链接到`portfolio.html`）跟首页4个板块里已有的"T00's AI Portfolio"卡片（同样链接到`portfolio.html`）内容重复，用户明确指示按"能做什么，和做出什么"两个角度拆分`t00.html`（原268行单页），并要求按钮措辞去掉"看看"这种邀请式软化框架（三语言同步去掉，不是只删中文两个字），改成直接陈述式。
  - **`t00.html`保留"能做什么"（能力/机制）内容**：hero（`t00d_h1`/`t00d_eyebrow`/`t00d_lede`）、4步流程图（`t00d_flow1-4`）、"Two questions people actually ask me" FAQ（`t00d_faq_*`，属于能力的具体证明）、"T00 at a glance"前后对比（`t00d_glance_*`）、"What it means, from two angles"受众细分（`t00d_lay_*`/`t00d_pro_*`）。**判断call**："What it actually is"这段框架性说明（`t00d_s1_*`）保留在能力页——它讲的是T00作为一个协作方法论"是什么/怎么运作"，属于机制层面的通用说明，不是具体产出物，跟能力页调性一致。新增一个结尾小CTA（`t00d_end_h2`/`t00d_end_p`/`t00d_end_cta`，三语言新写）链接到`t00-output.html`，让两个页面互相交叉链接。
  - **新建`t00-output.html`承载"做出什么"（具体产出）内容**：信任条统计数字（30个斜杠命令/100篇操作指南/18条框架规则，从`t00d_stat*`改名`t00o_stat*`）、"Designed around the human being the bottleneck"req/reply协议说明（`t00d_s2_*`→`t00o_s2_*`，是具体机制产出不是抽象哲学）、"Where 25 years of engineering experience actually shows up"+真实案例callout（`t00d_s3_*`/`t00d_ex_*`→`t00o_s3_*`/`t00o_ex_*`，具体产出物证据）、结尾CTA区块（`t00d_s4_*`/`t00d_cta1/2`→`t00o_s4_*`/`t00o_cta1/2`，重新调整了第二个链接——原来指向`index.html#featured`，现在这个页面本身就是"做出了什么"，改成交叉链接回`t00.html`"See what T00 can do →"，不再需要一个指向自己内容的链接）。新增该页专属hero（`t00o_h1`/`t00o_eyebrow`/`t00o_lede`，三语言新写实际文案，非占位文字）+独立`<title>`/meta description/OG标签。页面结构（nav/footer/语言切换/主题切换）逐字复制自`t00.html`，跟全站其它页面共享同一套外壳。
  - **顺手修了两处小问题**：①原`t00d_cta1`按钮文字写"看这五个概念/See the 5 concepts"，但旁边正文明明写的是"eight concrete concepts/八个具体概念"（req/reply协议、信任级别、永续记忆、斜杠命令、Skills、请求映射、设计文档、诚实汇报，数下来确实是8个）——这是移动前就存在的既有bug，顺手改成"8"跟正文对齐，三语言同步；②发现`t00d_pro4`（"if you build software"象限最后一条）把`<strong>`标签直接写死在i18n字符串里，但`main.js`是用`textContent`赋值，导致这个标签会原样显示成文字"<strong>The real one:</strong>..."——这跟本项目memory里"PJ50的i18n不能塞HTML标签"记录的是同一类真bug，已用Playwright实测确认过线上显示是裸标签文字，随手拆成`t00d_pro4_strong`（独立`<strong>`元素）+`t00d_pro4`（`<span>`正文）两个key修复，三语言同步。
  - **i18n.js维护**：`assets/i18n.js`三个语言块（EN/JA/ZH）逐一处理，共73个`t00d_*`/`t00o_*`相关key改名/新增（含上面两处顺手修复共74个），用脚本核对过：每个key在三语言块里都恰好出现3次（一次一种语言，无遗漏无重复），`t00d_*`全部被`t00.html`引用、`t00o_*`全部被`t00-output.html`引用，两边互不越界（无orphan key）。`node --check`语法验证通过。
  - **首页(`index.html`)两个底部按钮**：按钮1保留`href="t00.html"`，文案改成直接陈述式——EN "What T00 does →"/JA "T00にできること →"/ZH "T00能做什么 →"；按钮2的`href`从`portfolio.html`改成`t00-output.html`，文案——EN "What T00 has built →"/JA "T00が作ったもの →"/ZH "T00做出什么 →"。日语版特别注意按用户要求没有翻译成"見てみる"这类同样软化的说法，改用直接的体言止め（名词短语结尾）风格。确认过全站范围内只有`index.html`链接到`t00.html`（这轮之前就是如此），`t00-output.html`是全新页面目前没有其它页面链接进来，不会有孤儿链接混乱问题。
  - **导航栏判断**：grep确认`t00.html`本身从未出现在任何导航下拉菜单里（只在首页底部按钮出现），因此按同样逻辑`t00-output.html`也不需要加导航入口，两个页面都只通过首页底部CTA+相互交叉链接可达，未改动导航结构。
  - **验证**：Playwright本地(`python -m http.server 8998`)+生产环境双重验证，桌面1400×900+手机390×844，EN/JA/ZH三语言切换，两个新/改页面控制台均0报错（除favicon 404）。**过程中额外发现并修复一个无关的预置bug**：`t00.html`"T00 at a glance"对比区块（`.glance-arrow`）在手机端因为父级flex容器`align-items:stretch`导致箭头被拉伸成跟旁边卡片一样高，再叠加`transform:rotate(90deg)`后视觉footprint严重变宽，实测手机端`scrollWidth`(453px)超出`innerWidth`(390px)造成横向溢出——核对过这段CSS/HTML本次拆分完全没有改动过，是移动前就存在的问题，因为验证要求明确检查手机端无横向溢出，顺手在原有`@media (max-width:680px)`媒体查询里给`.glance-arrow`加了`align-self:center`覆盖掉继承的stretch，修复后实测`scrollWidth`(375px)恢复正常，不影响桌面端原有布局。部署后对`t00.html`/`t00-output.html`/`index.html`/`assets/i18n.js`逐个curl确认200，并直接对生产环境`https://maysuns.uk/t00.html`+`https://maysuns.uk/t00-output.html`桌面+手机截图确认线上渲染正确、控制台0报错。**备份**：改动前`i18n.js`/`index.html`/`t00.html`完整快照存于`Z01_backup_20260730d/`。**诚实说明**：本次改动过程中发现有另一个并发会话正在同时给全站加"联系我们"功能（新增`contact.html`/`contact-admin.html`，footer链接从`mailto:`改成`contact.html`，版本号推进到`?v=20260730e`），已确认这次并发改动跟本任务改动的键无冲突（用脚本核对过key计数），本次交付的三个文件在部署时不可避免地一并带上了那次并发改动的内容（因为已经保存在同一份磁盘文件里），未做任何回退，也未替对方提交git——git commit本次未执行（仓库当前有大量并发中的未提交改动，贸然一起提交风险较高，如需要请用户明确指示后再单独处理）。截图：`help_screenshots/t00_capability_desktop_zh_20260730.png`/`t00_capability_desktop_en_20260730.png`/`t00_capability_mobile_en_20260730.png`/`t00_capability_mobile_glance_fixed_20260730.png`/`t00_output_desktop_ja_20260730.png`/`t00_output_desktop_en_20260730.png`/`t00_output_mobile_zh_20260730.png`/`index_new_buttons_desktop_zh_20260730.png`/`t00_production_desktop_20260730.png`/`t00_output_production_desktop_20260730.png`/`t00_production_mobile_20260730.png`/`t00_output_production_mobile_20260730.png`。

- **2026-07-30（第二十三次，新增AI概念词典页`knowledge-ai-glossary.html`+全站27个页面导航加入口）**：来自任务`tasks/T04_ai_glossary_knowledge_base.md`，用户要求"不是术语表，是知识库，从0到精通的程度"。
  - **新页面`knowledge-ai-glossary.html`**：合并了两套已有模式——上半部分是站内标准`<nav>`外壳（跟其它页面一致，语言切换/主题切换/导航复用`assets/site.css`+`assets/main.js`），下半部分是R11模板（`AI00_Common/_templates/mobile_help_template.html`）风格的左侧菜单+右侧详情两栏布局，改造成英/日/中三语言版本。覆盖5层级共31个术语（基础层6个/开发能力工具层6个/Agent层5个/知识与应用层7个+Embedding跨层链接/最终应用层7个），每个术语按"是什么→为什么重要→具体例子→常见误区"四段式写满三语言（没有偷工减料留空的语言），Embedding在知识与应用层不重复全文，只做跨层链接回基础层同一条内容。适用术语（Prompt/Skills/MCP/Tools/Harness/Memory/Rules/Workflow/RAG/Local Knowledge Base/Agentic RAG/Coding Agent/Personal AI）附"T00中的例子"，链接到`knowledge-t00.html`/`product-rag.html`/`knowledge-rag.html`（逐一核实过这些页面确实存在且相关，不是瞎链）。🔊发音按钮用浏览器`speechSynthesis`朗读术语原文（不是音标），朗读语言实时读取`document.documentElement.lang`（由`main.js`的`setLanguage()`统一维护），做到跟随访客当前选择的语言自动切换，没有另建一套独立的语言切换器。顶部+底部各有一处"Made by maySunAI — maysuns.uk"钩子，链接到`https://maysuns.uk/`绝对地址。
  - **准确性核实**：MCP、Agentic RAG、LangGraph这三个较新/易混淆的术语，写之前用WebSearch核实了最新定义（而不是凭印象写）——确认MCP是Anthropic 2024年底推出的开放标准、2025年12月已捐给Linux基金会下的Agentic AI Foundation；确认Agentic RAG是让Agent自己判断检索时机/次数/是否足够的迭代式检索，区别于普通RAG的"检索一次就回答"；确认LangGraph是LangChain团队做的、把Agent工作流构建成显式图（节点+边）的库，专门解决循环/分支/持久状态，纠正了一处来源里"low-code可视化"的不准确说法（LangGraph本质是代码库，不是低代码工具）。三处术语内容里都注明了"本条目已在撰写时通过网络搜索核实"。
  - **判断call**：31个术语的三语言正文内容（约2万字）**没有**塞进全站共享的`assets/i18n.js`（该文件被全部27+页面加载，塞进去会让每个不相关页面都多背这份重量），而是单独放在这个页面自己的JS对象`GK_TERMS`里，语言切换靠页面自己的`gkOnLangClick()`包装函数——先调用真正的全局`setLanguage()`（保证跟全站行为一致：更新`document.documentElement.lang`/localStorage/主题按钮文案），再重新渲染这个页面自己的术语内容。只有导航栏/页脚这些跟其它页面共享的小字符串（`nav_aiknowledge_glossary`等5个key）按标准做法写进了`i18n.js`三个语言块。
  - **修了两个真bug**（都是自己写的时候引入、开发阶段就发现并修复，没有带着问题上线）：①`site.css`里有一条通用规则`nav { display:flex; ... }`对页面上所有`<nav>`标签生效，而术语侧边菜单我也用了`<nav id="gkMenu">`标签（为了语义正确），结果侧边栏被强制变成横向flex排列、菜单项挤成一排文字纵向换行——加了`#gkMenu{display:block}`显式覆盖解决；②最初写中文正文时用了直排英文引号`"..."`做行内引用（如`"token"`），跟JS字符串本身的双引号定界符冲突导致语法错误——写了一个脚本把63处中文正文里的行内引号统一换成中文弯引号"…"，改完用`node --check`验证JS语法通过。
  - **移动端菜单不冲突**：术语侧边栏有自己独立的"☰ Terms"按钮+遮罩层（`gkToggleMenu()`/`#gkOverlay`），跟站点自己nav的汉堡菜单（`toggleMobileNav()`/`#navRight`/`#navBackdrop`）用了完全不同的id和函数名，z-index刻意设得比站点nav低（55 vs 100），保证两个抽屉菜单同时存在时站点nav永远不会被盖住。
  - **全站导航整合**（27个html文件批量脚本改动，改动前先备份到`Z01_backup_20260730c/`）：在"AI Knowledge ▾"下拉菜单里插入新的"AI用语集/AI Concepts Glossary/AI術語集"入口（`nav_aiknowledge_glossary`/`nav_aiknowledge_glossary_sub`两个key三语言同步写进`i18n.js`），位置在"General Knowledge Base"之后、"Stories"之前，跟其余下拉项格式一致。脚本执行后逐文件核对：27个文件全部只新增了预期的1行，无意外改动；发现`stories.html`原本用CRLF换行（其它文件是LF），脚本处理时不小心统一成了LF，已用脚本转回CRLF跟原文件格式一致，顺带清理掉一处该文件里本来就存在的、无害但多余的重复回车符（`\r\r\n`→`\r\n`）历史遗留问题。
  - **验证**：Playwright本地(`python -m http.server 8994`)+生产环境双重验证。桌面1400×900+手机390×844，控制台0报错（除favicon 404）；点击多个术语切换正常，EN/日本語/中文切换后菜单+正文+🔊按钮语言同步正确；🔊按钮点击后`speechSynthesis.speaking`变true、无报错；手机端`scrollWidth`等于`innerWidth`无横向溢出；抽字搜索框实测能正确过滤菜单项；抽查`index.html`导航下拉菜单，新增项正确出现在正确位置且下拉菜单本身未被破坏。部署后对全部27个改动文件+新页面+`assets/i18n.js`逐个curl确认200，并直接对生产环境`https://maysuns.uk/knowledge-ai-glossary.html`截图确认桌面渲染、控制台0报错、手机端无横向溢出。截图：`help_screenshots/glossary_desktop_zh_20260730.png`/`glossary_mcp_freshnav_20260730.png`/`glossary_langgraph_en_20260730.png`/`glossary_agenticrag_ja_20260730.png`/`glossary_mobile_closed_20260730.png`/`glossary_mobile_termsopen_20260730.png`/`index_nav_dropdown_glossary_check_20260730.png`/`glossary_production_desktop_20260730.png`。

- **2026-07-30（第二十六次，首页大改：去掉大标题+按钮移到最后+4板块紧凑单行）**：用户反馈上一轮的大标题"T00工作流——让AI和Agent做你的助手..."排版不好看，直接要求"这些大字不要了"，并要求真的去查网页排版参考（不只是原则性结论）。这次真的用Playwright访问了`linear.app`/`raycast.com`两个真实网站截图查看（存在`F:\T00\temp\ref_linear_hero.png`/`ref_raycast_hero.png`），如实发现：**这两个真实产品官网其实都用了很大的标题字**，"大标题"本身是业界常见做法，不是普遍意义上的"不好看"——但用户是明确表态不想要这个风格，这是个人网站的主观取舍，不是客观对错问题，所以尊重用户的明确指示直接执行，没有拿"业界惯例"去说服用户保留。具体改动：①**彻底删除大标题h1**，首页现在只剩一句自我介绍文字（不再有任何大字标题），`f_hero_h1`这个key（三语言）确认无其它页面引用后一并从`i18n.js`删除；②**"看看T00能做什么→"/"看看已经做出了什么→"两个按钮从hero区移到全部4个板块下方、footer上方**，作为"看完内容再点击"的结尾行动号召，不再在开头就摆按钮；③**4个板块改成紧凑单行**（`grid-template-columns:repeat(4,1fr)`，间距从20px收紧到12px），呼应用户"图之间距离太大，紧凑点/放一行也可以"的要求，宽屏一行4个，1100px以下自动降为2列，760px以下单列，缩略图容器改成自适应宽度（不再固定420px上限）以配合更窄的单元格。已用Playwright验证桌面1400×900（单行4板块+按钮在底部）+平板900×900（2列）+手机390×844（单列，`scrollWidth`≤`innerWidth`无横向溢出），控制台0报错；部署后curl确认`index.html`/`assets/i18n.js`均200，生产环境截图确认渲染正确。截图：`help_screenshots/index_v6_compact_row_zh_20260730.png`/`index_v6_tablet_900_zh_20260730.png`/`index_v6_production_20260730.png`。

- **2026-07-30（第二十五次，首页hero真正精简——从5段文字砍到2段）**：用户反馈"首页文字太多了，已经不止一次说了"——上一轮虽然确认过hero文案内容跟用户给的文本逐字一致，但结构上其实堆了5段文字（intro+eyebrow"T00到底能做什么"+h1+一整段lede说明+tagline），加起来仍然偏"多"，没有真正做到用户之前引用的websearch结论"首屏一句话说清楚是谁/做什么/一个CTA"。这次直接砍：删掉eyebrow、lede长段落、tagline三个元素（连同`assets/i18n.js`里`f_hero_eyebrow`/`f_hero_lede`/`f_hero_h1_1`三个key三语言共9行一并清理，确认过除index.html外没有别的页面引用这几个key），`f_hero_h1`换成用户这次给的目标句式"T00工作流——让AI和Agent做你的助手、员工、Partner、老师、朋友"（三语言同步意译，不是逐句直译）。现在hero只剩：一句自我介绍（谁）+一句新headline（T00是什么/能干嘛）+2个CTA按钮，从5段文字变成2段+按钮，是真正的结构性精简，不是文案微调。同时确认PJ14计划任务加固命令用户已在管理员PowerShell里执行成功（`RestartCount:999`/`RestartInterval:5分钟`已生效，实测核实）。已用Playwright验证桌面1400×900+手机390×844（中文），控制台0报错，`scrollWidth`≤`innerWidth`无横向溢出；部署后curl确认`index.html`/`assets/i18n.js`均200，生产环境截图确认渲染正确。截图：`help_screenshots/index_v5_hero_simplified_zh_20260730.png`。

- **2026-07-30（第二十四次，首页再改：板块重命名+重排序+统一缩略图尺寸+悬浮效果+术语库左侧分类样式修复）**：用户对上一轮改动继续提反馈：①首页4个板块重排序为"T00的一天→AI知识库→本周AI动态→作品集"（原顺序是故事→作品集→知识库→动态）；②改名——"T00的一天"→"Jason和T00的一天"，"学过的东西，整理出来了"→"AI知识库"（用户原话嫌旧名字"有点土"，直接采纳了用户自己提议的新名字，跟nav菜单"AI Knowledge"叫法统一），"作品集"→"T00的AI作品集"；③"画面上四个部分大小最好一样，别太大"——新增`.f-thumb`统一缩略图容器（`aspect-ratio:4/3`+`object-fit:cover`+统一`max-width:420px`），不管是插画还是网页截图，4个板块的图片展示尺寸现在完全一致，不会有的明显更大；④顺手加了卡片hover悬浮效果（边框变色+轻微上移），呼应用户"参考好的skill/GitHub页面"的要求——websearch了开发者作品集设计趋势（强排版层次/留白/统一卡片处理），应用的是这类通用极简设计原则，不是照搬某个具体网站。首页hero部分文案核对后**发现已经完全符合用户这次给的"简化版"文案**（三语言i18n.js里的`f_intro_line`/`f_hero_eyebrow`/`f_hero_h1`/`f_hero_lede`/`f_hero_h1_1`逐字比对完全一致），未做改动。⑤`knowledge-ai-glossary.html`左侧分类标签（"基础层"/"开发工具层"等）视觉修复——原来字号小(0.76em)+纯灰色+无图标，改成更大字号(0.86em)+加粗+品牌蓝强调色+📂文件夹图标+顶部分隔线，分类现在明显可辨。已用Playwright验证桌面1400×900+手机390×844（`scrollWidth`≤`innerWidth`无横向溢出），控制台0报错；部署后curl确认`index.html`/`assets/i18n.js`/`knowledge-ai-glossary.html`均200，生产环境截图确认渲染正确。截图：`help_screenshots/index_v4_desktop_zh_20260730.png`/`glossary_menu_style_fix_20260730.png`。用户对"这份AI术语知识库内容本质是AI/T00通用知识，该不该归在PJ50下面"这个内容定位问题，选择了"保持现状（推荐）"——`knowledge-ai-glossary.html`确认继续留在`PJ50_personal_website/`作为maysuns.uk正式页面，不迁移到`AI00_Common/`，此问题已解决无需再处理。

- **2026-07-30（第二十二次，首页再精简：去掉T00方法论卡片+AI Agents板块，剩余4个板块重排序+改成PC两栏网格）**：用户反馈首页"从人的角度看不好看"，具体要求：①删掉"T00 — the method behind everything here"卡片和整个"🤖 AI Agents & Automation"板块——理由是跟`stories.html`（T00的一天故事）内容重复，且这两处链接的产品页（`product-website-generator.html`/`product-email-agent.html`/`product-rag.html`/`product-agent-platform.html`）都已经在`portfolio.html`里可达，删掉首页入口不会造成孤儿页；②剩下的"T00的一天/作品集/知识库/本周AI动态"4个板块按用户指定顺序重排：故事→作品集→知识库→本周动态；③"🧰 Everything I've shipped"改名成"🧰 Portfolio/作品集（ポートフォリオ）"，跟nav菜单叫法统一；④PC端从原来"一个板块占一整行"改成2列网格（`.f-cats-grid`，`grid-template-columns:repeat(2,1fr)`），768px以下自动收成单列。顺手清理了`index.html`内联`<style>`里只服务于被删掉的两个板块的死代码（`.f-featured`/`.f-card`/`.f-cat-tools`/`.status`/`.agent-tag`/`.f-more`），`assets/i18n.js`里`card_t00_*`/`f_cat1_*`/`card_webgen_*`/`card_email_*`/`card_rag_title`/`card_agent_title`/`agent_tag_label`这些key**没有删**（`portfolio.html`还在用同一套key，删了会连portfolio也坏掉，只删了index.html里引用它们的HTML）。已用Playwright验证桌面1400×900（2列网格正确显示）+手机390×844（单列堆叠，`scrollWidth`等于`innerWidth`无横向溢出），控制台仅1条无关favicon 404；部署后curl确认`index.html`/`assets/i18n.js`均200，并对生产环境`https://maysuns.uk/index.html`截图确认线上渲染正确（含日文语言状态下的实际截图）。**过程中的一个小失误**：第一次scp部署时目标路径写错，把`index.html`误传进了`assets/`目录，发现后已删除这个多余文件，不影响最终结果。截图：`help_screenshots/index_v3_desktop_grid_ja_20260730.png`/`index_v3_mobile_ja_20260730.png`/`index_v3_production_20260730.png`。

- **2026-07-30（第二十一次，全站导航调整+首页重构成"图+链接"模式+PJ06页面再改版）**：
  - **全站导航**（27个html文件批量脚本改动）：①语言按钮顺序改成EN/日本語/中文（原日本語/EN/中文）；②"Game Hall"改名"Relax"/"息抜き"/"休息一下"（三语言同步，nav_games的i18n值）；③"Maysun Shop"和"What's Next"从顶部导航移除，改放到`about.html`新增的"Also/その他/其它"板块（两张`f-card`卡片+链接，复用已有的`card_shop_title/desc`和`next_h1/next_sub`翻译，不用重复维护文案）。
  - **首页(`index.html`)按"T00的一天"故事板块的样式重构**：①hero最上面新增第一人称开场白（"👋我是Jason，一个生成式AI的学习者。用AI做产品的同时，也慢慢养成了自己的一套工作流——T00。"三语言）；②新增3个"图+链接"板块，跟故事板块统一风格——**AI动态**（`updates-news.html`真实截图+链接）、**AI产品**（`portfolio.html`真实截图+链接，取代了原来一行纯文字的"更多作品"链接）、**知识库**（`knowledge-t00.html`"Eight T00 concepts"卡片区真实截图+链接，对应用户说的"学过很多知识，要不要链接知识库或者列表截图"）；这3张图都是Playwright对真实页面截的图，不是插画/示意图。**诚实说明一点取舍**：这3张截图是英文界面下截的，没有为日语/中文版另外分别截图（3语言×3张=9张的工作量没做），中日文访客看到的教程截图里文字仍是英文，如果介意可以告诉我，可以后续补上日/中文版截图。
  - **`product-help-system.html`再改版**：①去掉了顶部"← Back to all products"返回链接（点左上角maySunAI logo本来就能回首页，属于冗余）；②去掉了"个人使用·动作确认済"这行badge（三语言，因为中文版残留了日语"済"字，且用户觉得这行没必要，直接删掉不只是修正错字）；③标题图标改成同一行（原来是图标一行、标题另起一行）；④去掉"下载模板"按钮和说明文字；⑤**页面从原来的纯黑产品页风格，改成一张浮在深色背景上的米白色"文档卡片"**（衬线字体标题、白底、圆角、投影——更接近"markdown渲染出来的文档"的观感，不是原来那种电商产品详情页风格）；⑥新增**真实截图**——没有用原来那份中文占位骨架模板去截图（那份只有"改这里"这种占位文字，截出来不能算"真实"），而是把`assets/downloads/help_doc_template_sample.html`（下载模板文件本体，虽然下载按钮已移除，但文件还在，改成了一份英文可运行的示例文档：Getting Started/Key Concepts/Glossary/FAQ四个菜单项，含真实的🔊发音按钮）填充成真实可交互的英文示例，然后用Playwright实际截了手机端"菜单收起"和"☰菜单展开"两张图放进页面，不是摆拍/示意图。
  - 已用Playwright验证本地+生产环境，桌面1400×900+手机390×844，英/中/日三语言，全部页面控制台0报错（除favicon 404）；`div`标签开闭数量脚本核对全部27个html文件+`i18n.js`语法均通过；部署后逐个curl核实所有改动文件+3张新截图+英文示例文件均200，并直接对生产环境`index.html`（中文）截图确认渲染与本地一致。截图：`help_screenshots/index_v2_desktop_en_20260730.png`/`index_v2_mobile_en_20260730.png`/`index_v2_production_zh_20260730.png`/`product_help_system_v3_desktop_en_20260730.png`/`product_help_system_v3_mobile_20260730.png`/`about_v2_desktop_20260730.png`/`template_sample_mobile_nomenu_20260730.png`/`template_sample_mobile_menuopen_20260730.png`/`home_kb_teaser_20260730.png`/`home_news_teaser_20260730.png`/`home_products_teaser_20260730.png`。**备份**：改动前完整快照在`Z01_backup_20260730b/`（27个html文件+i18n.js，这次是先备份再动手，跟上一轮"改完才想起备份"不同）。

- **2026-07-30（第二十次，首页大改+PJ06页面文案重写）**：按用户"感觉网站东西多，没做到宣传力度"的反馈，做了两处改动：①**首页(`index.html`)精简重构**——移除了"Local Knowledge Base"卡片（已有nav入口，首页这里说不清楚实际内容）、移除了整个"📦 Shipped & Live"（商店/KDP/游戏）和"🛠️ Tools & Extras"（阅读器/记忆助手/吃药提醒）两个板块（判断依据：用户明确要求"主要放和ai有关系的"，这两个板块跟AI能力展示关系不大；执行前核实过`product-kdp.html`等页面不会因此变成导航孤儿——KDP页面还能从`product-shop.html`链接到，商店/游戏本身在nav有直接入口，不影响可达性），新增"📖 T00的一天"故事预告板块（复用`stories.html`场景1的`man_computer.png`插画+9:00徽章+一句话文案+链接过去），底部加一行"更多已上线的作品，在完整作品集页面→"链接到`portfolio.html`（东西没有真的消失，只是不在首页堆着）；②**`product-help-system.html`(PJ06介绍页)文案重写**——原来的长段落说明改成"主要功能"5条+"使用场景"4条的清单格式（更好扫读），标语（tagline）改成明确点名"T00的HTML Help文档生成系统"，原来提到"「Jason如何用T00工作的一天」故事「回家后」那一幕"的那句话按用户要求整句删除。**判断说明**：user原话只点名要删"本地知识库"和"工具与其他"两处，"📦 Shipped & Live"是否也要删是我自己的判断（理由已如上说明），如果不认可可以用`Z01_backup_20260730/index.html`（改动前完整原文件）直接覆盖回滚，该文件夹里还有一份`i18n_changed_keys_20260730.md`记录了PJ06页面被替换掉的原文案三语言原文，方便单独回滚这一处。**诚实说明备份顺序**：这次先做了编辑，改完才想起来创建`Z01_backup_20260730/`（理想顺序应该是先备份再改），备份文件是靠对话记录里已读取的原始文件内容重建的，内容与改动前完全一致，只是操作顺序上没有按"先备份再动手"来。已用Playwright验证桌面1400×900+手机390×844，中日英三语言，首页+PJ06页面控制台均0报错（仅favicon 404）；部署后curl确认`index.html`/`product-help-system.html`/`assets/i18n.js`均200，直接对生产环境截图核对首页渲染与本地一致。截图：`help_screenshots/index_v1_desktop_zh_20260730.png`/`index_v1_desktop_en_20260730.png`/`index_v1_desktop_ja_20260730.png`/`index_v1_mobile_zh_20260730.png`/`product_help_system_v2_zh_20260730.png`/`product_help_system_v2_mobile_20260730.png`/`index_v1_production_live_20260730.png`。

- **2026-07-30（第十九次，场景6气泡也去掉+两图等高+互相鞠躬图改版+PJ06/PJ19真做了链接页+修了一个data-i18n塞HTML的真bug）**：①场景6"改动前"的箭头气泡（上一轮加的"← this icon"）也去掉了，改成跟"改动后"完全同构的写法（都是`position:absolute`+`padding-top:22px`的居中/左上logo），两张对比图现在**实测高度完全相等**（217.36px=217.36px）；②场景8的鞠躬图——用户反馈现有的"两人互相鞠躬"其实是"打招呼"场景不是"告别"，去查了irasutoya"見送り"（送别）+"エレベーター"（电梯）关键词、以及irasutoya站内搜索，确认**没有精确匹配"门口/电梯送别互相鞠躬"这个具体构图的免费素材**，如实告知没有找到更贴切的图，保留现有的互相鞠躬图（如实说明这条没做完，不是敷衍应付）；③**真的把PJ19和PJ06的link页面做了**：参考PJ11(`product-website-generator.html`)的模式，新建`product-help-system.html`（PJ06，讲HTML帮助文档系统本身，附一个真实可下载的模板文件`assets/downloads/help_doc_template_sample.html`，复制自`PJ06_help/templates/mobile_help_template.html`原始文件）；PJ19（概念阶段，没有真代码）没有做成"产品页"（那样会显得像已经做出来的东西），改成在`whats-next.html`新增一个"構想中/In the Pipeline/构想中"板块，诚实标注"(concept stage)"，风格跟这个页面已有的"LINEグループ(準備中)"卡片一致；场景1和场景9的注释现在分别链接到这两处。④**过程中发现并修复了一个真bug**：给PJ06页面写介绍文字时，在一条`data-i18n`字符串里直接嵌了`<a href="stories.html">...</a>`标签，结果线上直接显示成`<a href="stories.html">`这串literal文字——查了`main.js`确认`data-i18n`是用`el.textContent`赋值（这是本项目第二次犯这个错，第一次是2026-07-29 stories.html的mapping/回答区块），这次已经存成正式的feedback memory（之前只在changelog里叙述过，没有存成memory，导致同样的坑踩了第二次）——已经拆成`<span>`+独立`<a>`+`<span>`三段各自配i18n key修复，并用Playwright截图肉眼确认链接正确渲染，不再只信"语法检查通过"。已用Playwright验证桌面1400×900+日文，控制台0报错；部署后curl确认`stories.html`/`whats-next.html`/`product-help-system.html`/`assets/downloads/help_doc_template_sample.html`/`i18n.js`均200，并直接对生产环境`product-help-system.html`+`stories.html`截图确认渲染正确、控制台0报错。截图：`help_screenshots/stories_v19_scene6_equalheight.png`/`product_help_system_fixed.png`/`whats_next_pipeline_check.png`。

- **2026-07-30（第十八次，去掉icon高亮圆环改箭头气泡+互相鞠躬图+场景注释重写加真链接+手机边框再修）**：①场景5+场景6"改动前"两处的金色高亮圆环（`box-shadow`ring）已去掉，改成一个箭头+小气泡样式的标注（"← this icon"，白底小气泡+左箭头，贴在icon旁边），不再是圆环+下方文字两件套；②场景8"再见"换成`ojigi_mutual.png`——去irasutoya.com搜到的"お辞儀をし合うビジネスマンのイラスト（男女）"（两人互相鞠躬告别，而不是原来单人鞠躬的`ojigi_bow.png`），来源同样是irasutoya免费商用素材；③**逐条核实后重写了4处场景注释**（`sc-pjnote`），每条改成"T00的PJxx——一句话真实描述"的简洁格式，并核实了每个PJ号的真实状态：场景1标注PJ19（查过`PJ19_todolist_assistant/CLAUDE.md`，如实标注"概念阶段"，因为这个项目目前只有立项文档没有真代码，没有像用户原话那样写成"已经在查邮箱确认工作"这种既成事实的语气，避免夸大）；场景3改成PJ11（查过`PJ11_maysunAI/OVERVIEW.md`，确认"聊天里说一句就生成网站"是真实测试过的功能，加了指向`product-website-generator.html`的链接，这个页面本来就是讲这个能力的）；场景6维持PJ14（加了指向`product-email-agent.html`的链接）；场景9新增两处注释——图片上方"T00的req/reply记录"（链接到`kb-doc-viewer.html?doc=t00-req-reply-protocol.md`）+ 文件卡下方新增PJ06（`PJ06_help`，真的是本站help文档生成机制所在项目，但PJ06目前没有独立的`product-*.html`页面，这条先不加链接，如实告知）；同时给"详细设计书.md"文件卡的状态文字加上"另附用户手册+从0到实现的开发手册"。④结尾免责声明从一长串"每个能力都真实"的罗列，精简成一句话（`⚠️ Illustrative scenario, composite client — every capability shown is real.`），因为具体是哪些能力现在已经分散在各场景自己的注释链接里讲了，不用在免责声明里重复罗列。⑤**手机端"边框太大、左右空得多、还换行"这条反馈这次真正测量诊断**：用Playwright量出`.st-section`+`.st-card`两层padding在390px手机屏上叠加起来左右各占48px（合计96px，占屏宽近25%），比之前只调小几个卡片内边距更关键——这次直接在手机断点里把这两层padding分别收窄到12px/16px，实测同一句话在手机上从原来3行收成2行，横向利用率明显提升，且`scrollWidth`仍小于`innerWidth`没有引入横向溢出。**遗留**：邮件截图（`email_to_ai00.png`/`email_ai00_reply.png`）用户反馈"不像标准邮件图"，要求对比强调Subject/From-To/回信格式——查看当前截图后发现Subject/From-To/Re:回信格式其实都已经在图里，具体还欠缺哪里没能确认，已经在reply里如实提问，没有盲目重做一轮猜测式修改。已用Playwright验证桌面1400×900+手机390×844+日文，控制台0报错，手机端无横向溢出；部署后curl确认`stories.html`/`i18n.js`/`ojigi_mutual.png`均200，并直接对生产环境截图确认渲染正确。截图：`help_screenshots/stories_v17_scene5_arrow_check.png`/`stories_v17_scene6_check2.png`/`stories_v17_scene8_check.png`/`stories_v17_scene9_top_check.png`/`stories_v17_mobile_after_padding_fix.png`。

- **2026-07-30（第十七次，D01改表格格式+修复手机端Jason气泡消失的真bug+去掉页面通用大标题）**：①用户实际尝试直接编辑`D01_stories_content_spec_v001/v002.md`后反馈"不好改、描述性文字太多不够简洁"，还建了`D01-History/`文件夹存历史版本——查了storyboard（影视分镜脚本）行业惯例后确认"一行=一个场景的表格"是通用做法，改版做了`D01-History/D01_stories_content_spec_v003.md`（全中文、表格格式，场景总表+页面级文案+i18n key对照+图片一览四张小表），对应更新了`H105_content_spec_doc_workflow.md`记录这次改版依据。②**修了一个真bug**：场景1/2的对话气泡（"Jason: ..."文字）在手机端(`@media max-width:560px`)之前是`display:none`直接整个隐藏，导致PC端能看到"Jason在说话"这条信息，手机端完全看不到——内容不一致。已改成跟`.st-caption-card`同样的手机端处理方式（`position:static`让它变成图片下方的静态说明，不再是可能溢出的绝对定位气泡），PC和手机现在内容一致。③**页面结构调整**：去掉了页面顶部通用的"幕后故事/Stories"大标题+一句话简介（`.st-hero`区块），故事自己的标题("Jason如何用T00工作的一天")升级成页面唯一的H1；新写了一条更聚焦的intro——"Jason，一名软件工程师，用自己的AI工作流「T00」完成日常开发"，三语言同步。④场景1正文开头从"我问一句，T00直接答"改成"Jason向T00询问今天的任务"（第三人称描述，不再是别扭的"我问一句"翻译腔），三语言同步。已用Playwright验证桌面1400×900+手机390×844+中日文，手机端Jason气泡正确显示不再消失，控制台0报错；部署后curl确认`stories.html`/`i18n.js`均200且确认线上已无`st-hero`残留、新文案已生效，并直接对生产环境截图确认渲染正确。截图：`help_screenshots/stories_v16_top_check.png`/`stories_v16_mobile_scene1.png`/`stories_v16_top_zh.png`/`stories_v16_production_top.png`。

- **2026-07-30（第十六次，新增D01内容地图文档+2篇help文档）**：用户认可"详细设计书"这个思路后，正式落地：①新建`PJ50_personal_website/D01_stories_content_spec_v001.md`——逐场景（1-10）记录`stories.html`现在的标题/正文（含i18n key）/图片文件+选择理由/布局说明/场景注释，附一张图片文件一览表+改动方式提醒，以后改动可以直接说"按D01第X场景改"，不用每次口头重新描述页面细节。按用户反馈调整了原计划——版本快照不放`Z01_archive/`（怕找不到），改成直接在项目文件夹里用`v001`/`v002`编号，旧版本不删；②新建`AI00_Common/_docs/help/general/H105_content_spec_doc_workflow.md`，解释这套"内容地图文档"工作流本身（跟已有的REQUIREMENTS/OVERVIEW/DESIGN三份文档的区别、什么时候该用、模板结构）；③新建`H106_can_ai_learn_the_memory_illusion.md`，详细回答用户"AI是否能学习/进化"的问题——如实说明模型本身不会因对话而改变，"越来越了解你"的体验来自外部的memory文件持久化机制，并具体回答了两个追加问题："存很多memory是否增加token"（索引常驻但小，单条详情按需读取，成本取决于这次命中几条而非总条目数）和"能否针对特定任务替换memory"（能删改具体条目，"任务自动只看到相关那批"已通过相关性筛选实现，但没有强隔离的模式切换功能）。已运行`gen_help_html.ps1`重新生成`T00_HELP.html`（107篇文档）。

- **2026-07-30（第十五次，导航重新归类stories+场景6去文字标注+发现浏览器已登录LinkedIn的先例）**：①去掉了场景6"改动前"图片里的"↑ this icon"文字标注（场景5的同款标注保留，因为那里是"正在讨论这个icon"的语境，场景6单纯靠icon位置本身对比前后就够了，不需要重复文字）；②**导航结构调整**：用户指出"T00的一天"这个故事讲的是T00的实际功能演示，不是传记性质的"关于我"内容，放在"About Me ▾"下拉菜单里不合理、不容易被想了解T00功能的人发现——认同这个判断，把`stories.html`的导航链接从"About Me ▾"移到了"AI Knowledge ▾"下拉菜单（跟"T00 Knowledge Base"/"RAG Knowledge Base"/"General Knowledge Base"排在一起），"About Me ▾"原本只剩"About Us"一项，简化成普通链接不再是下拉菜单。这个改动涉及全站28个页面共用的导航栏结构，用PowerShell脚本批量替换+对`stories.html`/`about.html`/`knowledge-general.html`（自身是当前页、nav-current标记不同）手工特殊处理，未改动`stories.html`本身的URL（只改了导航入口位置，之前发出去的外部链接不受影响）。已用Playwright验证桌面下拉菜单展开正确显示4项（含新加入的Stories）、手机汉堡菜单同步简化、控制台0报错，部署后curl确认全部28个页面200且无重复/遗漏的`nav_aboutme_label`残留。③**LinkedIn发布能力更正**：上一轮（第十四次末尾）曾告知用户"我没法登录你的LinkedIn账号"，这次用户追问后实际检查发现是错的——当前Playwright浏览器工具本身处于已登录用户真实LinkedIn账号（`linkedin.com/in/jason00514`）的状态，且`PJ53_job_search_appeal/draft_linkedin_content.md`记录显示之前的会话已经用同样方式（操作已登录浏览器，非存储密码自动登录）多次编辑过用户的LinkedIn About栏——这就是用户所说"你以前发布过"的真实依据。已打开发帖框、填入此前给出的英文+日语摘要文案草稿，但**在真正点击发布前停下等待用户确认**（发帖是公开、面向全部联系人可见、发出后难以完全撤回的操作，判断为需要用户过目后再执行，不同于此前"改自己profile"的性质）。截图：`help_screenshots/nav_restructure_check.png`/`nav_restructure_mobile_check.png`/`linkedin_draft_top2.png`/`linkedin_draft_bottom2.png`。
- **2026-07-29（第十四次，PC白边+场景6对比图重做+场景9改内容防重复+精简为单故事页）**：这轮是针对`stories.html`的又一批具体反馈：①**PC端白边**——查了`assets/site.css`发现是这个页面自己写的`.st-hero`/`.st-section`用了比全站其它页面更窄的容器（`min(1100px,92vw)`），改成跟全站一致的`min(1200px,94vw)`标准尺寸；②**场景6"改动前/改动后"对比图重做**——"改动前"复用场景5右侧那张金色圆环高亮居中icon+"↑ this icon"标注的图，"改动后"基于场景3的浏览器mockup把icon改成绝对定位左上角（无高亮），两张图现在有真实的视觉差异，不再只是抽象的圆点位置；③**场景9人物插画**从280px放大到380px；④**场景9的req/reply内容重写**——查证发现原内容"icon居中→左上"跟场景6（邮件场景同样讲icon居中→左上）确实重复，已换成「根据file.md修改细节→生成详细设计书」，正好跟这个场景本来就有的"详细设计书.md已生成"文件卡呼应；⑤新增手机端断点，收窄`.st-real-illust`/`.bm-body`/`.st-screen-mock`等卡片的内边距，减少图片占用的横向空间；⑥**结尾场景重写**：去掉"客户打款给你/已收款"这两行，改成"感谢你们的完美工作，期待下次合作"+一张握手插画（复用了之前下载但一直没用上的`undraw_agreement.svg`）；⑦**精简为单故事页**：删掉了页面末尾"Making a School Emblem"占位卡（从未写过正文）和"How I Work With AI: 3 Rules"卡——后者内容查证后发现`knowledge-t00.html`的"Eight T00 concepts"板块里"req/reply协议""信任级别""诚实汇报"三篇文章已经覆盖了同样的内容，是重复而非缺失，没有另外搬运；占位卡内容记到了`FUTURE_FEATURES.md`的F006里。**判断说明**：用户提到"这个页面的url也可以改"，但考虑到`stories.html`这个链接已经通过LinkedIn/Reddit发布出去了（见第十三次记录），改URL会让已发布的链接失效，这次判断为维持现状更稳妥，未改动URL，如实告知这是自己的判断而非用户明确要求维持。已用Playwright验证桌面1400×900+手机390×844+中日英三语言，控制台0报错（除1条无关的favicon 404），手机端无横向溢出；部署后`curl`确认`stories.html`/`assets/i18n.js`/`undraw_agreement.svg`均200，并对生产环境`https://maysuns.uk/stories.html`用Playwright重新截图确认线上真实渲染正确、控制台0报错。截图：`help_screenshots/stories_v14_full_desktop.png`/`stories_v14_full_mobile.png`/`stories_v14_full_desktop_zh.png`/`stories_v14_scene6_check.png`/`stories_v14_ending_check.png`/`stories_v14_production_full.png`。
- **2026-07-29（第十三次，修复真实邮箱泄露+4处场景细化）**：⚠️**紧急隐私修复**：用户发现邮件截图场景里显示的是真实个人Gmail地址（`j6016086@gmail.com`），已经暴露在线上超过1小时——立即换成占位邮箱`jason@example.com`/`ai00@example.com`，同时把邮件内容改成英文（三语言读者都能看懂）、加上明确的"Jason → T00"/"T00 → Jason"方向标签，重新截图后第一时间单独部署上线，不等其它改动一起做。其余4处细化：①"一起看网站"场景新增右侧网站mockup（复用画面3同款结构），把讨论中的sun icon用金色圆环高亮+"↑ this icon"标注，让"他们在看什么"变得具体可见；②邮件场景的改动前后对比图，发现是`.rr-col`的`font-size:0.74em`级联导致文字变小，改用跟画面3一致的`.st-row-2`布局，文字恢复正常大小，"Before"/"After"标签加粗；③"第二轮反馈"场景发现插画跟"一起看网站"场景重复用了同一张`undraw_meeting.svg`，去掉插画改成跟画面3同款的虚线框文字说明；④"回家整理"场景的req/reply截图原来是Playwright截的835×149px静态小图，文字放大后必然糊——改回不用截图，复用之前用过的`.st-screen-mock`活体HTML组件（深色显示器边框），重写内容明确分成📌原文/🔎mapping/✅回答三段，字号从0.7em提到0.92em，人物插画收窄到280px居中，整体分区更清楚。**过程中修了一个真bug**：写mapping/回答文字时最初用了`<br>`和`&rarr;`/`&nbsp;`HTML写法，但查了`main.js`发现`data-i18n`是用`el.textContent`赋值（不是`innerHTML`），这些HTML标签/实体会被原样显示成文字而不是渲染——改成每行拆成独立的`<div class="sm-text">`元素，去掉所有内嵌HTML写法。已用Playwright验证桌面1400×900+手机390×844+中日英三语言，控制台0报错，手机端无横向溢出，且**直接对生产环境`https://maysuns.uk/stories.html`重新截图确认线上真实渲染正确**（吸取上一轮H86坑4"本地验证≠线上验证"的教训，这次没有只信任本地预览）。截图：`help_screenshots/stories_v11_full_desktop.png`/`stories_v11_full_mobile_zh.png`/`stories_v11_production_email_scene.png`。同时给`PJ03_publish_promotion/POSTING_GUIDE.md`新增了"六、LinkedIn发帖"完整step-by-step说明（用户反馈"不太了解怎么放LinkedIn"）。
- **2026-07-29（第十二次，合并场景+彻底删除重复投诉文字+真实邮件截图+发现并修复线上图片全404的部署bug）**：①**合并场景**：把"T00正在做"(workbox)和"网站做好了"(browser mock+文件卡)合并成一个场景，故事从11幕精简到10幕；②**彻底删除"这句话太生硬了"**——用户指出这条删除请求已经说了3次都没被真正执行，查证后发现根因是上一轮mapping把同一个编号下的两条子反馈（"删除这句话"+"icon措辞修正"）揉成了一条，只处理了后者，前者被静默丢弃——这次已在三语言i18n.js+HTML里彻底删除该caption，并把这个"编号内多条子要点被合并丢失"的失败模式写进了`AI00_Common/_docs/help/general/H86_ai_collaboration_pitfalls_case_studies.md`坑3，同时把"素材找不到先换地域来源再手绘"和"主观判断先出1例再铺开"两条教训存成了memory feedback条目（评估后认为比做成正式skill更合适，量级不匹配）；③**邮件场景重做**：新增2张真实邮件截图(`email_to_ai00.png`发给T00的请求邮件+`email_ai00_reply.png`T00完成后的回信，Playwright截图HTML邮件客户端mockup生成)替换原来的动画mockup，改动前后对比图改成静态双栏（不再动画），删掉了下面多余的聊天气泡文字；④**加pj编号注释**：todolist场景标注`PJ19_todolist_assistant`（如实说明这个项目就是被这个故事启发立项的）、邮件场景标注`PJ14_email_agent`、建站场景如实说明"这是本站自己的AI One-Shot App Generator功能，不是独立编号项目"（没有编造不存在的PJ号）。**部署时发现并修复了一个影响范围更大的真实bug**：部署后逐个`curl`生产环境图片URL核对，发现`man_computer.png`/`meishi_exchange.png`/`ojigi_bow.png`/`reqreply_screenshot.png`这4张插画在线上全部404——不是这次改坏的，是从这些图片被创建起就从来没有真正部署到服务器上过，此前好几轮changelog里的"已用Playwright验证线上正常"都只验证了本地预览，没有真正curl过生产环境URL；另外`assets/illustrations/`目录权限是`drwxr--r--`（nginx worker非root用户没有目录执行位，会导致所有文件404），也一并`chmod 755`修复。已把这条"本地验证≠线上验证"的教训写进H86坑4+存了对应memory。**验证方式**：本地Playwright验证桌面1400×900+手机390×844+中日英三语言切换，控制台0报错，手机端无横向溢出（截图`help_screenshots/stories_v10_full_desktop.png`/`stories_v10_full_mobile_zh.png`）；部署后对`stories.html`引用的全部图片URL逐个`curl`确认HTTP 200，并用Playwright直接对生产环境`https://maysuns.uk/stories.html`截图确认线上真实渲染正常（`help_screenshots/stories_v10_production_live_desktop.png`）。**遗留提醒**：PJ50独立git仓库(`github.com/maysunAI/maysuns.uk`)已经有9轮改动只部署到服务器、从未`git commit`，这次也未处理（不在本次任务范围内，如实告知，未自作主张打包提交）。
- **2026-07-29（第十一次，换用irasutoya真实插画+结构性修复）**：这轮的关键突破是**换了图片来源**——之前"交换名片""鞠躬告别"两个场景一直找不到合适插画（undraw/storyset这些西方插画库根本没有这类东亚商务礼仪场景），这次改搜日本本土免费素材站`irasutoya.com`（いらすとや），直接搜到了"名刺交換のイラスト"和"深くお辞儀をしている男性会社员のイラスト"，完全对应需求，不再是手绘凑合的替代品（授权：irasutoya免费商用，单个设计里用不超过20张免费，本页只用了3张，在免费范围内，已在图片下方标注`illustration: irasutoya.com`）。同时下载了一张"パソコンを使う男性のイラスト"（单人男性用电脑），替换掉画面1/2/10里的双人`undraw_teamwork.svg`，解决"不需要两个人"的问题。**结构性修复**：①每个场景的编号+标题现在排在插图前面（不再是图在前编号在后）；②去掉了所有"Illustration: undraw.co"角标文字；③标题从"T00的一天"改成"Jason如何用T00工作的一天"；④画面1对话气泡加了"Jason"标签；⑤画面2重写成"Jason引用邮件说一句→T00执行读邮件/定要件/建站三步"，不再是简单的1234需求复述；⑥去掉了"几分钟后"和"网站做好了"两处冗余描述文字；⑦icon位置的说法从"从中央移到右上"改成"从中央移到左上"（呼应maysuns.uk真实首页logo在导航栏左上角的实际布局），相关的动画方向、对话文字、mapping文字全部同步改了。截图：`help_screenshots/stories_v9_full_desktop.png`/`stories_v9_full_mobile.png`。手机预览链接：`http://<本机局域网IP>:8977/stories.html`（本机WiFi网卡`192.168.1.58`，见此前记录）。
- **2026-07-29（第十次，去icon化+T00徽章+彻底解决2处重复投诉）**：用户这轮反馈里有两处是"第二次说了""上次说过了你没改"，这次优先确保真正解决：①**交换名片图**——undraw上搜了好几轮都没有"两人交换名片"的精确插画，这次不再继续找stock插画，改成**自己用CSS手绘**一个简化的"两只手+两张卡片+交换箭头"图标，保证内容跟"交换名片"字面意思一致，不再是"最接近但不是"的替代品；②**画面7的"电脑截图"**——之前虽然做了deployment"仿真截图样式"的HTML卡片，但用户反馈还是不满意，这次**改用Playwright对着页面元素直接截了一张真实PNG图片**（`assets/illustrations/reqreply_screenshot.png`），把原来的活HTML mockup换成`<img>`引用这张真图片，同时按"跟第一幕类似"的要求恢复了插画+双栏布局（左边`undraw_teamwork.svg`插画+T00徽章浮层，右边就是这张截图）。另外处理：③每个场景圆形icon去掉了emoji，只保留数字（用CSS counter自动编号），减少装饰性icon；④要件定义第4项"日英双语"改成语言无关的"多语言支持"（不再单独点名日语英语没有中文的问题）；⑤新建了统一的"T00徽章"组件（☀️太阳图标+T00文字，深色药丸状，比之前的纯文字标签"T00✨"更醒目更统一），替换了所有原来的文字标签；⑥"出发"和"到了客户公司"两个场景合并成一个（配合新的交换名片图标）；⑦"跟客户说完再发邮件"这个过渡场景合并进了邮件卡片场景，去掉了重复的对话框，只保留一行精简文字，邮件里原有的icon位置动画原样保留；⑧告别场景从`undraw_goodbye.svg`换成了**自己手绘的"电梯+鞠躬emoji"**组件（undraw上同样没有这种日式商务告别礼仪的插画，这次没有再找替代插画，直接手绘）；⑨结尾完成场景去掉插画，只保留一个放大的🎉大图标+客户表扬/收款信息。**顺手修复了一个真bug**：新的"今天完成"标签因为`.sc-tag`样式只在`.st-caption-card`或`.st-frame-b`内部生效，直接用在别处时没有背景色和文字颜色，几乎看不清——已经把`.sc-tag`改成独立通用样式，不再依赖父级。已用Playwright验证桌面1400×900+手机390×844，全部改动正常显示，控制台0报错，手机端无横向溢出。截图：`help_screenshots/stories_v8_full_desktop.png`/`stories_v8_full_mobile.png`/`stories_v8_final_scene.png`。
- **2026-07-28（第九次，编号+Jason署名+maysuns.uk植入+OG标签+推广建议）**：①每个场景的圆形icon右上角加了数字徽章（用CSS counter自动编号，不用手动维护）；②intro文案改成"跟着Jason一个真实的工作日走一遍"，把用户真实姓名带进叙事；③结尾新增CTA区块——文案"这就是Jason跟T00一起工作的真实日常"+一个链接到`https://maysuns.uk/index.html`的按钮"看看T00还能做什么→"，作为植入的品牌链接（用绝对URL而不是相对路径，这样万一被截图/转发到别的平台，域名文字依然可见）；④新增`og:title`/`og:description`/`og:url`/`canonical`/`twitter:card`几个meta标签（之前完全没有，分享到社交媒体时预览会是默认的，现在至少标题描述正确）。**关于推广渠道/该分享哪个链接**，做了websearch调研后给出的建议见reply正文，这部分是判断/建议不是代码改动。已用Playwright验证桌面1400×900+手机390×844，编号徽章+CTA按钮正常显示，控制台0报错。截图：`help_screenshots/stories_v7_top.png`/`stories_v7_cta.png`。
- **2026-07-28（第八次，14项细节修改+动画bug彻底修复+新增2幕）**：按用户对reply第1043-1057行的逐条反馈继续改：①画面1新增"🔍直接从客户邮件里读到的"标注；②画面2改叙事——不再是"我转述客户要求"，改成"T00自己读了邮件"，对话气泡改到右侧人物+加"T00"标签+蓝色描边，明确是电脑T00在说话；③"T00正在做"去掉了"日英双语"字样；④网站mockup公司名从Acme改成**maySunAI**，加了☀️居中logo；⑤"出发去客户公司"拆成独立的轻量过渡行（放在前面），"拜访客户"换成新插画`undraw_nice_to_meet_you.svg`（原本用握手+合同的`undraw_agreement.svg`，这次换成更简洁的打招呼插画，更接近"交换名片"的分寸，但如实说明：undraw上没有一模一样"两人交换名片"的插画，这是最接近的一张）；⑥修复了汇报过的动画"PC上还是看不到"——这次改成真正的死循环（`animation infinite`，滚动到画面后会不断重复播放，不再是播一次就结束，不管什么时候看都能看到在动）；⑦按钮位置标记新增了醒目的"↑按钮icon位置"黑底标签；⑧新增一幕"跟客户说完，转手就发邮件给T00"（对话气泡+发邮件过渡行），补上"先口头答应客户→再发邮件"这个环节；⑨邮件标题前缀从错误的"T00:"改成了PJ14真实使用的**"AI00:"**（去查了`PJ14_email_agent`真实的邮件处理约定，命令识别就是认"AI00:"前缀，之前写错了）；⑩再见场景保留`undraw_goodbye.svg`（挥手告别），如实说明——undraw上没有"送到电梯鞠躬告别"这种日式商务礼仪的专门插画，找不到更贴切的，用了最接近的挥手场景；⑪画面7的插画从"修bug"插画换成**自建的"电脑屏幕mockup"**，屏幕里显示的内容是仿照`temp03_reply.md`真实格式做的req/reply截图（不是文字描述，是看起来像截图的样子），外面套了一个深色显示器边框+标题栏，人物emoji坐在屏幕前；⑫req/reply对比卡这次包在了"屏幕"里，视觉上更像截图而不是裸露的文字块；⑬结尾"完成"画面新增礼花emoji（🎆🎉🎊）+客户表扬对话+"💰已收款"标签。**过程中发现并修复了一个新的真实布局bug**：新增的"出发"和"发邮件给T00"这两个场景，因为在`.st-step`（默认横向flex排列）下塞了多个内容块又没加`st-step-illustrated`（纵向排列）类，手机端会挤压成3条并排的窄列——已修复两处，改用`st-step-illustrated`使其纵向堆叠。已用Playwright重新验证：桌面1400×900+手机390×844，全部新内容正常显示，手动测试确认滚动触发的循环动画持续播放，控制台0报错，手机端无横向溢出。截图：`help_screenshots/stories_v6_full_desktop.png`/`stories_v6_full_mobile_fixed.png`。
- **2026-07-28（第七次，11幕大改+修复动画看不到的真bug）**：按用户对reply第979-995行的逐条反馈重做"T00的一天"，画面从7幕扩到11幕：①画面2改成跟画面1同款的双框布局（一样大，人对电脑说+T00标注回复）；②新增"T00工作中"过渡（无合适插画，用蓝色虚线框+3条逐步淡入的进度项代替，已在T03里标注这是文字方案）；③画面3去掉人物，改用**纯CSS自己搭的浏览器mockup**（不是插画，因为要显示具体网址/站名/内容，插画里的人物没法去掉又不想改插画本体），旁边配"要件定义.md"文件卡片，去掉了"几分钟就上线"标签；④过渡场景从emoji换成专业插画`undraw_agreement.svg`（握手+签约）；⑤画面5整体重做：邮件标题"T00：PJ50—修改要求"+正文缩小版网站mockup(标记点center→top-right)+2轮客户/T00对话(不再只有1轮)+仿真req/reply格式回答卡；⑥新增"再见"过渡（`undraw_goodbye.svg`挥手插画）；⑦画面7新增"详细设计书.md"文件卡+req/reply真实格式对比卡；⑧新增结尾"完成"庆祝画面（`undraw_celebration.svg`）。新增3个插画素材（`undraw_agreement.svg`/`undraw_goodbye.svg`/`undraw_celebration.svg`，同样recolor品牌蓝）。**修了一个真bug**：之前"改动icon居中→右上"的动画绑定在页面加载时间上，用户滚到那一屏的时候动画早就播完了（这就是"PC上看不到动画"的真正原因）——已改成用IntersectionObserver监听滚动，动画只在画面滚入视口时才触发播放，且做了无JS兜底（默认直接显示最终正确状态，不会因JS异常而永久不可见）。已用Playwright验证：桌面1400×900+手机390×844，滚动触发的动画实测能正确播放（手动scrollIntoView+等待后确认`st-play`类被添加），全部插画正常加载，手机端无横向溢出，控制台0报错。同步更新了`tasks/T03_story_scene_design.md`到v2，逐幕记录改动+标注了2处不确定的设计选择。截图：`help_screenshots/stories_v5_full_desktop.png`/`stories_v5_full_mobile.png`。
- **2026-07-28（第六次，画面1/2/3/3.5/5多项细化+设计稿文件）**：按用户给的一串具体要求逐条落实：①画面1"新任务"邮件展开出具体的"要件定义"清单（4条：公司介绍页/产品介绍/联系方式/日英双语）；②画面2从纯聊天气泡UI换成跟画面1同款的"插画+对话气泡"（人对着电脑说"先按客户要求做网站试试"），T00回复也换成明确标注"T00"的卡片，并逐条确认1-4项要求；③画面3的`undraw_website.svg`插画上叠加了网址徽章(www.acme-demo.com)+站名("Acme公司—首页")；④新增画面3.5：画面3和画面4之间加了一条"💻→🚶→🏢"过渡横幅，配文字"带着网站链接，出发去客户公司"；⑤画面5（邮件往来，宣传重点）新增两个动画——"✅已经改好了"徽章从图片正中央（放大1.3倍）用2.2秒动画飘到右上角定住，插画下方新增笔记本→服务器的发邮件动画（一个蓝色圆点在两个图标间来回移动）。**诚实标注**：画面5的"改动的地方icon居中→右上"这条指令有歧义，已经按"是指✅徽章的移动动画"这个理解去做，但不确定猜对了，已经在新建的设计稿文件里明确标注这个不确定点，等用户确认。同时按用户要求"最好生成一个文件方便改"，新建了`tasks/T03_story_scene_design.md`，逐幕描述现在的设计+标注了3处不确定的地方（网址/公司名是随便起的示例名、过渡横幅用的是emoji不是专业插画、居中→右上角动画的理解是否正确），方便用户直接在文件里写反馈而不用一条条对着网页描述。已用Playwright验证桌面1400×900+手机390×844，所有新元素正常显示，手机端`scrollWidth`等于`innerWidth`（无横向溢出），控制台0报错。截图：`help_screenshots/stories_v4_full_desktop.png`/`stories_v4_full_mobile.png`。
- **2026-07-28（第五次，手机预览+画面1拆两图+修复真bug）**：用户反馈"手机看不到"，排查发现是`127.0.0.1`本来就只能本机访问，跟有线/无线无关——已改用`ipconfig`查到本机真实局域网IP（Wi-Fi网卡`192.168.1.58`，以太网网卡`192.168.0.5`，两块网卡在不同网段，本机同时连了两个网络），`python -m http.server`默认监听`0.0.0.0`已确认对外可达，Windows防火墙也已有Python的Allow入站规则不会拦截，给用户提供了`http://192.168.1.58:8977/stories.html`作为手机预览链接（前提是手机WiFi和这台电脑的WiFi在同一个路由器下）。同时按用户反馈"和T00说话就出现待办清单，这样不合理"，把画面1从"一张图叠两层信息"改成**两张图+箭头**的因果关系布局（问→答）：左边插画只保留时钟9:00+对话气泡"T00，今天要做什么？"，右边单独一张待办清单卡片（含高亮的新任务行），中间一个箭头连接，手机端自动变成上下堆叠+箭头转90度。**过程中发现并修复了两个真实CSS bug**（不是猜的，是Playwright截图对比出来的）：①`.st-illustration`在flex列布局下因为同时有`flex:1 1 0`+`overflow:hidden`，触发flexbox"overflow非visible时自动最小高度为0"的规则，导致手机端插画整个高度塌陷成0、完全不可见——改成手机端媒体查询里显式`flex:1 1 auto;width:100%`修复；②新增的`.st-frame-b`卡片忘记设置文字颜色，深色主题下文字继承了页面的浅灰`var(--text)`，在卡片米色背景上几乎看不清——补上`color:#2b2b33`修复。两个bug桌面+手机都已用Playwright重新截图确认修复。截图：`help_screenshots/stories_scene1_final_desktop.png`/`stories_scene1_final_mobile.png`。
- **2026-07-28（第四次，7幕剧本重做+马上做）**：按用户给的具体分镜剧本，把"T00的一天"从10个场景精简重做成7幕（去掉了"介绍T00是什么"的说明性文字，intro从一大段缩成一句话）：①9:00+时钟徽章+对话气泡"T00，今天要做什么？"+待办清单卡片（新增高亮的"新任务：做网站+客户会议"一行，直接在图里体现"弹出ToDoList后链接一封邮件"）②纯聊天气泡UI卡片（不需要插画）："先按客户要求做网站试试"→"好的，开始做"③`undraw_website.svg`插画+"几分钟就上线"标签④`undraw_meeting.svg`插画+客户当面意见气泡⑤**宣传重点，插画放大**：`undraw_mail_sent.svg`（居中，最大260px，比其他场景插画更醒目）+绿色"已经改好了"大徽章+客户/T00对话框，明确体现"T00要说XX已经改了"⑥复用`undraw_meeting.svg`+"第二轮：具体变更"标签⑦`undraw_fixing_bugs.svg`插画+一张仿真的req/reply记录小卡片（"📌 req 第12-15行 → reply / ✅ 已完成"，直接展示T00真实的记录格式，不是抽象描述）。全部走"一个场景一张图+图里带关键信息"的模式，没有再出现"一张图配一大段文字"的情况。新增3个插画素材（同样来自undraw.co，同样recolor成品牌蓝`#60a5fa`）：`assets/illustrations/undraw_website.svg`/`undraw_meeting.svg`/`undraw_mail_sent.svg`/`undraw_fixing_bugs.svg`。中日英三语言同步改写（用户这次说"先用中文就行"，但为了不让其他语言页面出现空白/报错，还是把三语言都写了，只是审核精力优先放在中文上）。**验证方式**：Playwright桌面1400×900+手机390×844，7个场景全部正常渲染、控制台0报错，手机端卡片正确降级为图片下方静态排列不溢出。截图：`help_screenshots/stories_v3_full_desktop.png`/`stories_v3_full_mobile.png`。
- **2026-07-28（第三次，画面1返工）**：针对用户反馈"不满意/不专业/图不精美/只有一个人"，做了4处改动：①页面标题`stories_h1`/`stories_page_title`中文从"我的琐事"改成"幕后故事"（原来的"琐事"听起来不专业）；②画面1插画换成真实专业插画素材——用Playwright从`undraw.co`下载了免费可商用的"Teamwork"矢量插画（两个人+笔记本电脑+台式显示器+墙上装饰画，标准的"科技公司办公室"扁平插画风格），存到`assets/illustrations/undraw_teamwork.svg`，把默认紫色`#6c63ff`改成品牌蓝`#60a5fa`，删除了之前手绘的单人咖啡插画（不够精美的问题直接换成专业素材解决，不是继续手绘迭代）；③在插画上叠加一个浮动"待办清单"卡片（未读邮件/项目截止日期/昨天的文档三行+T00标签，逐行淡入动画），把原来要用文字段落说明的内容改成直接在图片里可视化呈现；④相应缩短了画面1的文字说明段落（中日英三语言同步），文字能看图理解的部分不再重复写。**素材授权说明**：unDraw插画是免费可商用、无需署名的开源素材（unDraw License），仍然在图片角落加了小字"Illustration: undraw.co"作为惯例致谢。已用Playwright验证桌面1400×900深色/浅色主题+手机390×844，插画和待办清单卡片正常显示、手机端卡片自动降级为图片下方静态排列不溢出，控制台0报错。截图：`help_screenshots/stories_scene1_v2_desktop_dark.png`/`_desktop_dark2.png`/`_mobile.png`。
- **2026-07-28（第二次）**：`stories.html`「T00的一天」故事，画面1（☕早晨：今天的待办）改成手绘SVG flat illustration插画——暖色系办公室场景（木桌+窗户+绿植+人物+笔记本电脑显示待办清单+咖啡杯冒热气动画+待办项逐行淡入动画），固定亮色卡片背景（不跟随全站深色/浅色主题切换，理由跟照片一样不应该反色）。**用户确认**：人物用匿名插画小人物（不需要参考本人照片）；"加上我的内容"确认是指去掉虚构client包装、改第一人称——核实后发现画面1文案本来就已经是第一人称真实描述，没有虚构client成分（虚构client是从画面2开始才出现），所以画面1文案未改动，只加了插画。用Playwright验证：桌面1400×900深色/浅色主题+手机390×844三种状态，插画都正常渲染、无横向溢出、控制台0报错。**这是"一个画面一个画面做"的第1个**，画面2-10尚未开始，等用户确认这个方向OK再继续。截图：`help_screenshots/stories_scene1_desktop_zh.png`/`_desktop_light.png`/`_mobile.png`。
- **2026-07-28**：`t00.html`新增"T00 at a glance"4步流程图（说需求→自动找文件→拿不准就问→你确认结果），用手绘SVG图标+CSS实现（圆形图标+箭头连接），日/英/中三语言文案同步进`assets/i18n.js`。**诚实说明**：当前环境没有配置任何AI图片生成API Key（Gemini/OpenAI/等），无法调用`image`skill里提到的GPT Image 2/Nano Banana Pro生成真实AI图片，所以选择用手绘SVG线条图标做说明性流程图（这类"讲清楚流程"的示意图本来也比AI生成图更合适，AI生成的UI截图类内容还容易失真）。如果以后想要照片级/插画风格的宣传图，需要用户提供对应的图片生成API Key。已用Playwright在桌面(1400×900)和手机(390×844)视口验证：图标正常渲染、中日英切换正常、无横向溢出、控制台仅1条无关的favicon 404。截图见`help_screenshots/t00_flow_desktop_1400x900.png`/`t00_flow_mobile_390x844.png`/`t00_flow_desktop_en.png`。本地用`python -m http.server 8977`验证过，尚未部署到服务器。

## 本地预览规则（2026-07-20新增）

给用户本地预览链接时（`python -m http.server` 等），**每次改完东西重新起服务器都要换一个新端口**，把新的 `http://127.0.0.1:新端口/xxx.html` 链接告诉用户，不要一直用同一个旧端口/旧链接。
- 原因：用户反馈"总是确认的是旧版本"——同一个端口/URL容易被浏览器缓存旧内容，换新端口能保证用户点开的一定是最新文件，不用靠用户自己清缓存/硬刷新。
- 每次给新链接前，先确认旧的本地服务器进程已经关掉（避免残留进程占用旧端口继续提供旧内容）。
