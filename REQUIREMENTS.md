# REQUIREMENTS.md — PJ50_personal_website（原PJ44_personal_website，maysuns.uk）

## 原始需求（2026-07-08，用户原话）

> "maysuns.uk 是根目录，需要你更新内容。你全权处理。多个目的，a.找工作， b，吸引投资，c。加关注，引流 等等，是不是一个目的一个文件夹？"

用户明确给予「全权处理」授权，可做实际内容/结构决策，无需逐条确认；但不得部署上线（如有Vercel等部署步骤留给用户），不得改动任何支付/API Key/密钥相关配置。

## 结构问题的回答（本次任务的核心决策）

**问题**：是否应该"一个目的一个文件夹"（找工作/投资/引流三个目的各自独立）？

**决定：不采用"一个目的一个文件夹"的结构。改为"单一个人品牌叙事 + 每个受众独立锚点区块（anchor section）"的单页结构。**

理由：
1. 这是一个**个人网站**，不是企业官网。个人网站的核心资产是"这个人是否可信、是否讲得清楚一个连贯的故事"——如果把找工作/投资/引流做成三个完全独立的文件夹/页面，容易变成三套互相矛盾或重复的自我介绍，反而稀释可信度。
2. 但三类受众（招聘方/投资人/关注者）确实各自需要不同的"第一屏信息"和不同的行动号召（CTA）——招聘方想看到"能不能马上上手"，投资人想看到"值不值得聊"，关注者想看到"有没有持续更新的内容"。**完全揉在一段话里也不合适**。
3. 折中方案（本次采用）：**同一个单页网站，共享同一套背景故事（About区块：25年SE + 2023年起的AI自学历程），然后为三类受众各设一个独立的锚点区块**（`#recruiters` `#investors` `#follow`），每个区块有针对性的文案和CTA按钮，首屏（Hero）下方直接放一排"快捷入口"（Hiring? / Investor? / Just following?）让访客一眼跳到自己关心的区块。
4. 这与"真实网站往往需要面向不同受众的独立落地区块/页面，但共享同一个身份"的实践一致，同时避免了"一个目的一个文件夹"可能带来的叙事割裂。

## 技术方案（沿用原方案，未变更）

- 纯 HTML + CSS + 少量内联 JS（单文件 `index.html`，无框架、无构建步骤）
- GitHub Pages 托管（未改动部署配置，本次任务未执行任何 push/部署操作）
- Cloudflare DNS 绑定 maysuns.uk（`CNAME` 文件内容未改动）

## 本次内容更新范围（2026-07-08）

1. **Hero 区块**：从"AI-Powered Apps Built for Everyone"（纯产品向）改为同时呈现"Jason Gao 本人 + maySunAI 品牌"，并在 Hero 下方新增三个受众快捷入口。
2. **新增 About 区块**：25年SE背景 + 生成AI自学历程时间线（2023年12月自学Python惜败认证考试 → 2025年两次Python认证通过 → 2026年自建产品"记忆助手" → 2026年6月起T00框架AI/Agent开发冲刺）。时间线数字与 `PJ53_job_search_appeal/draft_resume_ai_focused.md` 保持一致（已核对信息来源，详见该项目 `PROGRESS.md`）。
3. **Portfolio 区块扩充**：在原有3个产品卡片基础上，新增 AI/Agent 类目（本地RAG知识问答应用、个人AI Agent平台、邮件驱动任务Agent、记忆助手），与已上架/开发中类目并列展示，让"技术深度"和"已交付产品"两条线都可见。
4. **新增 For Recruiters 区块**：面向招聘方的定向文案+CTA（申请简历/LinkedIn联系）。
5. **新增 For Investors 区块**：面向投资人/协作伙伴的定向文案+CTA，语气克制（不使用"高速增长""独角兽"等夸大措辞），与 `PJ53/draft_investor_pitch.md` 的定位一致。
6. **新增 Follow 区块**：面向"引流/加关注"目的，强调"公开构建过程（build in public）"的内容角度（包括诚实展示2023年考试失败的细节），并列出 LinkedIn/X/Facebook/GitHub 链接。
7. **联系邮箱统一**：将站内联系邮箱从原来的 `j6016086@gmail.com`（该邮箱经查为用户的 Google Play 开发者账号联系邮箱，见 `AI00_Common` 用户记忆）改为 `gao616188@gmail.com`（用户主邮箱，也是 `PJ53_job_search_appeal` 全部简历/求职材料统一使用的联系邮箱），使跨渠道（简历/LinkedIn/个人网站）联系方式保持一致。
8. **语言**：站点主体语言由声明的 `lang="zh"`（但原内容实际是英文，存在标签与内容不一致的问题）统一为英文 `lang="en"`，作为面向国际招聘方/投资人/关注者的主语言（日语相关求职内容仍保留在 PJ53 的日语简历/求职信中，个人网站不重复维护双语版本，避免维护成本过高）。

## 未解决/需要用户后续处理的事项

1. **X（Twitter）／Facebook 的真实链接尚未知晓** —— Follow 区块中这两个链接目前是占位符（`href="#"`，并加了 `title` 提示"发布前请替换为真实链接"）。请用户提供真实账号链接后再替换，**发布前必须处理**，否则会是死链接。
2. **本次未部署/未推送到 GitHub**，也未修改任何 Cloudflare/GitHub Pages 配置。若用户确认内容满意，需要自行执行 `git add/commit/push` 并在 GitHub 仓库设置中确认 Pages 已启用（步骤参考 `CLAUDE.md` 原有"部署步骤"）。
3. **简历文件未直接嵌入网站**：For Recruiters 区块采用"发邮件索要简历"而非直接放简历下载链接/文本，因为 PJ53 的简历草稿目前仍在用户核对中（时间线刚做过修订），在用户最终确认数字/年份之前，不适合把未定稿内容原样公开发布到网站上。待 PJ53 简历定稿后，可考虑在此区块加一个正式简历下载链接。
4. **未新增 PJ20 认知症支援App 等所有 T00 产品**：Portfolio 区块做了取舍，只展示了与 PJ53/PJ51 简历中提到的、且已有较强证据支撑的几个项目，避免网站信息量过大、失焦。如需展示更多项目，可再讨论增加。

## 2026-07-16 更新（手机邮件任务，投资人/求职向重做+补充产品线）

用户手机邮件原话："我的主页 能重新做一个吗，放在pj23服务器上。 需要建立pj就用pj5x的。以前有一个主页，可以放一起。主要找投资人和找工作用。最主要的是宣传产品t00，pj10，pj90等等。不要暴露核心做法。"

- **决定不新建PJ5x项目**：本次任务前一次执行（11:13那条同内容邮件）因300秒超时被杀死时，曾意外建了一个空文件夹`PJ54_founder_homepage`（无任何内容，只是空目录），本次已删除。用户原话"以前有一个主页，可以放一起"本身就是"别拆分、合并到一起"的意思，PJ50正是那个"以前的主页"，且已经是投资人+求职双重定位——不需要另建项目，直接在PJ50基础上补内容更合理。
- **Portfolio区块新增两张产品卡片**：MyAI个人AI助手平台（PJ90，进行中）、Product Shop商品直销平台（PJ23，已上线，附真实链接`shop.maysuns.uk`）。T00框架本身已有独立的"The T00 Framework"区块做整体介绍，本次未改动该区块。
- **"不要暴露核心做法"**：新增的两张卡片延续原有卡片风格，只写产品定位/技术栈名称（React/Vite、Cloudflare Workers、Stripe等已公开的技术选型），不涉及任何prompt设计、AI协作方法论、T00内部工作流细节。
- **⚠️ "放在pj23服务器上"这一条本次未执行**：PJ50目前的实际托管是GitHub Pages + Cloudflare DNS（`maysuns.uk`），跟PJ23生产服务器（Vultr `167.179.106.32`，nginx+pm2，已跑着真实付费商城`shop.maysuns.uk`）是两套完全独立的基础设施。把主页迁移/追加到PJ23那台生产服务器上，涉及登录生产服务器改nginx配置（可能还要买一个新子域名或复用现有域名），这会影响到已经在正常服务真实付费客户的生产环境——按照"改动共享生产设施前先确认"的原则，这一步本次没有直接执行，留给用户确认后再做（内容已经准备好，随时可以部署）。

## 2026-07-18 更新（邮箱订阅入口用邮箱）

用户提供了用于"邮箱订阅/联系入口"功能的邮箱：**主：`j6016086@gmail.com`，备用：`gaosong0514@outlook.com`**。

**冲突已解决**：用户明确决定，以后 `j6016086@gmail.com` 就是 maysunAI 品牌专用邮箱（`gao616188@gmail.com` 邮件太多容易看漏消息，不适合当品牌联系邮箱）。核实后发现 `index.html` 里实际使用的一直就是 `j6016086@gmail.com`（第326/348/380行），跟"2026-07-08"那条记录里写的"改成`gao616188@gmail.com`"不一致——**当时的文档记录有误或后续被覆盖过，实际代码从未改成`gao616188`**。现在用户的决定正好和现状一致，不需要改代码，只需要更新这份文档记录，以`j6016086@gmail.com`为准。`gaosong0514@outlook.com`暂时只记录备用，未展示到网站上。

## 2026-07-18 更新（首屏改版：产品优先，个人信息移到About）

用户实际打开`maysuns.uk`确认后反馈"下面显示的还是老的个人信息"，要求首屏改成产品优先、个人介绍移到About区块，"马上改"。

- **首屏(Hero)重写**：原来的标题"25 Years an Enterprise Engineer. Now Building With AI."+个人背景段落，改成产品导向的"AI-Built Products. Live, Working, Real."+简短说明，不再是第一屏看到的内容
- **区块顺序调整**：Portfolio（产品展示）移到Hero之后、About之前——访客现在滚动一屏就能看到产品，而不是先看一大段个人履历。原有的完整About内容（25年经验+时间线+谦逊说明）**完整保留**，只是挪到Portfolio后面，内容一字未改
- **导航栏顺序**同步调整为"Portfolio → About → ..."，跟内容顺序一致
- **meta description**同步改成产品导向的描述（原来也是"25 years..."开头，SEO/链接预览也会显示老信息，一并改了）
- **"每个页面能回到主页"**：给已经部署上线的两个子产品加了返回`maysuns.uk`的链接，并重新构建+部署：
  - `games.maysuns.uk`（PJ27）：大厅页顶部加了"← maysuns.uk"链接，静态HTML直接scp到服务器生效
  - `reader.maysuns.uk`（PJ25）：header里加了"← maysuns.uk"链接（`src/App.jsx`），`npm run build`后部署到服务器，新JS bundle hash已更新确认生效
  - **`shop.maysuns.uk`（PJ23）这次未动**：真实在跑Stripe付费交易的生产电商站点，风险和敏感度更高，这次没有主动改，需要的话请明确说一声再处理

## 2026-07-18 更新（中/日/英三语言切换）

用户原话："maysuns.uk 需要中日英。介绍我的，可以少一点。或者放一起。"

- **三语言切换实现**：不引入任何框架/构建步骤，延续"单文件HTML"技术路线。用 `data-i18n="key"` 属性标记全部需要翻译的文本节点（导航栏、Hero、Portfolio全部10张产品卡片、About、T00 Framework、Recruiters、Investors、Follow、Contact、`<title>`、meta description），配合内联 `<script>` 里的 `translations = { en: {...}, zh: {...}, ja: {...} }` 字典对象，`applyLanguage(lang)` 函数遍历 `[data-i18n]` 节点替换文本。
- **语言切换器位置**：导航栏右侧，主题切换按钮左边，三个小按钮「中文 / 日本語 / EN」，当前语言高亮显示（`.lang-btn.active`）。
- **默认语言判定**：`detectDefaultLang()` — 优先读 `localStorage.getItem('lang')`（记住用户上次选择）；否则用 `navigator.language` 前缀判断（`zh*`→中文，`ja*`→日语，其余→英文默认）。切换语言后立即写回 `localStorage`，刷新页面保留选择。
- **`<html lang="">` 属性同步**：切换语言时同步更新为对应的 `en` / `zh-CN` / `ja`，便于浏览器/搜索引擎正确识别页面语言。
- **中文/日文文案非机器直译**：Hero、About、Recruiters、Investors、Follow等营销向文案均按目标语言的表达习惯重新组织语序和措辞（例如英文的"start here"系列口语化短句在日语版改为更礼貌的敬语表达，中文版保留口语化但符合中文招聘/投资语境的说法），不是逐词翻译。
- **About区块精简**（本次同批次处理"介绍我的可以少一点"要求）：原来两段独立段落（① 25年经验+资质，② AI自学心路历程，合计约108词）合并压缩为**一段**（约70词，压缩约35%），删除了"团队规模最多5人""这份支撑我走过25年的踏实劲头"等可省略的修饰性表述，保留核心事实（25年经验、Oracle/DB2/AWS迁移、PMP/OCP认证、三语能力、2023年底起自学AI、约两年半自主实践）。三语言版本（en/zh/ja）都做了同等程度的压缩，时间线（4条bullet）保留不变——精简的是段落散文，不是关键节点信息。完整的25年经验细节仍完整保留在 For Recruiters 区块（`rec_p1`），About区块现在只是"精简版引子"。
- **本地语法校验**：用 `node --check` 提取并校验了内联 `<script>` 块，语法通过；HTML `<section>`/`<div>` 标签开闭数量一致（各自7对/46对）。

## 交叉引用

- 时间线/背景数据来源：`F:\T00\PJ53_job_search_appeal\draft_resume_ai_focused.md`（本次同批次修订，详见其 `PROGRESS.md`）
- 定位与语气一致性参考：`F:\T00\PJ53_job_search_appeal\draft_investor_pitch.md`（投资人区块语气对齐）、`F:\T00\PJ53_job_search_appeal\outreach_plan.md`（招聘方向策略）
- 日本AI岗位市场背景：`F:\T00\PJ51_job_market_jp\job_market_report.md`、`t00_gap_analysis.md`
